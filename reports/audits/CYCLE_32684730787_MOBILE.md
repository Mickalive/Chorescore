# Audit indépendant — candidat MOBILE, cycle 1 (run GitHub 32684730787)

- **Candidat** : `08f2a78` « ChoreScore 32684730787: mobile snapshot », worktree `/tmp/chorescore_mobile`
- **Base du candidat** : `d593d60` (ancêtre de la branche acceptée `lab/chorescore` à `337b89a`)
- **Diff audité** : `git diff d593d60..08f2a78` — 6 fichiers, +394/−9
- **Décision : `accepter`**

## 1. Périmètre réel du diff

| Fichier | Nature |
| --- | --- |
| `src/components/ContributionBar.tsx` | ajout de `accessible` pour exposer le pourcentage au lecteur d'écran |
| `src/components/MetricCard.tsx` | regroupement libellé/valeur/détail en un seul point de focus (`accessible` + label combiné) |
| `src/components/SegmentedControl.tsx` | rôles `radiogroup`/`radio` avec `accessibilityState.selected` |
| `src/components/TaskFormModal.tsx` | `autoFocus` sur le champ Nom ; chips de catégorie en `radiogroup`/`radio` |
| `src/domain/validation.ts` | `normalizeTaskName` : caractères de contrôle C0/DEL/C1 remplacés par une espace au lieu d'être supprimés |
| `tests/tab-flows.test.ts` | nouveau fichier, 12 tests couvrant les quatre onglets |

Périmètre conforme au rôle mobile (`app/`, `src/`, `tests/`). Aucune dépendance,
aucun lockfile, aucune orchestration, aucun fichier `.github/` modifié. Le
fichier `.github/scripts/run-opencode-with-retry.sh` n'apparaît que dans le diff
à deux points contre `lab/chorescore` : il appartient au commit accepté
postérieur `337b89a`, absent du candidat car parti de `d593d60`. Aucun risque de
retour arrière si l'intégration fusionne ou applique le diff trois-points.

## 2. Vérifications par thème

**Exactitude produit et score canonique.** Aucune formule modifiée. Le test
nouveau vérifie `(2700 / 60) × 7 = 315` via `DemoAppService.completeTimer`, ce
qui correspond à `calculateScore` (`src/domain/scoring.ts:19`). Les assertions
chiffrées du classement (60/40 parts, somme arrondie à 100), de la fenêtre
gratuite de 30 jours et du graphique quotidien (7 points, J−2 = 10 min brutes,
6 zéros) ont été recalculées contre `leaderboard.ts`, `periods.ts` et
`scoring.ts` : toutes exactes. L'arrondi reste réservé à l'affichage.

**Dark patterns.** Aucun. Le paywall testé s'ouvre sans changer le scénario ni
simuler un achat ; `autoFocus` est neutre ; aucune urgence fabriquée.

**Accessibilité.** Les changements vont dans le sens du canon (§4) :
radiogroup/radio cohérents avec `accessibilityState`, regroupement MetricCard en
une annonce unique, exposition effective du label de ContributionBar. Régression
lecteur d'écran non identifiée ; rendu visuel inchangé.

**Validation.** La correction `normalizeTaskName` est réelle et minimale :
l'ancien comportement supprimait `\n` (« Arroser\nles plantes » →
« Arroserles plantes ») ; le nouveau insère une espace puis collapse les
espaces. La plage `\u007F-\u009F` (DEL + C1) ne déborde pas sur des caractères
imprimables. Un nom uniquement composé de contrôles reste rejeté (< 2
caractères). Le code reste côté démonstration ; aucune décision serveur n'est
déplacée vers le client.

**Démo hors ligne.** `src/services/index.ts` reste figé sur `DemoAppService` ;
aucun import Firebase ni appel réseau dans `src/` (scan grep négatif). Aucune
référence réseau ni secret dans les fichiers du diff. Export Android démo
réussi.

**Isolation, autorisations objet, concurrence, rejeu, Stripe.** Hors périmètre
de cette tranche strictement client/démo ; rien d'activé, rien de contourné.
Les refus métier testés (double chrono, tâche inconnue, membre inconnu, chrono
déjà terminé) correspondent bien aux implémentations de `appReducer.ts`.

**Tests.** Le nouveau fichier démontre ce qu'il prétend : chaque assertion a
été confrontée au code source réel, fixtures comprises (`user_noa`,
`user_camille`, `household_rivage`, `task_dishes` existent dans
`demoData.ts`). Aucun test préexistant affaibli : `tests/validation.test.ts`
n'est pas touché par le diff (son intitulé « retirés » est désormais légèrement
imprécis face au nouveau comportement « remplacés par une espace », mais son
assertion reste vraie — constat cosmétique préexistant, hors responsabilité du
candidat).

## 3. Checks réellement exécutés (worktree candidat)

1. `git -C /tmp/chorescore_mobile merge-base lab/chorescore HEAD` → `d593d60` ;
   inspection de `git diff d593d60..HEAD`.
2. `npm run check` (Node v22.13.0) → typecheck OK, **58/58 tests passent,
   0 échec** (dont les 12 nouveaux).
3. `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android`
   → export réussi, bundle `entry-d83e44c85cd6bc4d1ab46ec3f1041033.hbc`.
4. `npm audit --omit=dev --audit-level=high` → code sortie 0 ; vulnérabilité
   `uuid` modérée transitive via expo uniquement, inchangée par le candidat.
5. Scans grep sur le diff et `src/` : aucun `fetch(`/URL réseau, aucun motif de
   secret, aucun `EXPO_PUBLIC_*` autre que la convention documentée, aucun
   import Firebase côté client.

Installation non relancée (`node_modules` monté par le workflow, lockfile non
modifié par le candidat) — conforme à la politique « scripts d'installation
désactivés ».

## 4. Constats

Aucun constat critique, élevé ou moyen. Deux observations mineures, non
bloquantes :

1. **Info** — `tests/validation.test.ts:5` : intitulé « les caractères de
   contrôle sont retirés » devenu imprécis après le passage à la substitution
   par espace. Fichier non touché par le candidat ; peut être reformulé lors
   d'une prochaine tranche tests.
2. **Info** — `src/components/TaskFormModal.tsx:69` : `autoFocus` dans une
   modale RN peut être inopérant sur Android selon le timing de la modale.
   Sans dépendance fonctionnelle ; acceptable en l'état.

## 5. Risques résiduels

- L'accessibilité réelle (TalkBack/VoiceOver, grandes tailles de texte) n'est
  pas vérifiable par checks déterministes seuls ; validation manuelle restante
  avant toute livraison store.
- `npm audit` reflète une base d'avis évolutive, pas une preuve de
  reproductibilité (rappel du workflow).

## 6. Décision

**`accepter`** — tranche petite, bornée, dans le périmètre autorisé, sans
contradiction avec le canon, sans régression hors ligne, tests sincères et
verts, aucun secret ni dépendance nouvelle. Intégration par le directeur selon
son périmètre habituel ; les deux observations mineures peuvent être traitées
plus tard sans bloquer le cycle.
