# Audit indépendant — MOBILE, cycle 32781937768 (round 1)

- **Candidat** : `/tmp/chorescore_mobile`, commit `9d29981` (« ChoreScore 32781937768: mobile snapshot »), branche trouvée : oui.
- **Base acceptée comparée** : `lab/chorescore` à `de6470b` (« ChoreScore cycle 32692689814: audited accepted state »). Le merge-base est exactement `de6470b` : le candidat est un commit unique au-dessus de l'état accepté, sans historique réécrit.
- **Décision : accepter.** Aucun constat critique ou élevé ; trois constats faibles/informationnels non bloquants, transmis au directeur.

## 1. Périmètre réel du diff

`git diff --stat de6470b..9d29981` : 3 fichiers, +451/−10.

| Fichier | Nature |
| --- | --- |
| `src/domain/history.ts` (nouveau) | sélecteurs purs `filterHistoryEntries` et `buildHistorySynthesis` |
| `app/(tabs)/history.tsx` | câblage UI : deux `SegmentedControl`, synthèse, note de plan, états vides |
| `tests/history.test.ts` (nouveau) | 7 tests node:test des sélecteurs purs |

Aucun fichier interdit (`MAIN_PROMPT.md`, directives hors périmètre, `.github/`, `.opencode/`, lockfiles), aucune dépendance ajoutée, aucune modification d'orchestration. C'est exactement la priorité mobile du cycle (`docs/NEXT_CYCLE.md` priorité 2 : « synthèses semaine/mois et filtres de l'Historique, sélecteurs purs testés »).

## 2. Exactitude produit

- **Filtres jamais bloquants ni paywallés** : `filterHistoryEntries` opère sur la fenêtre déjà visible produite par `getVisibleHistory(..., entitlements.historyDays, ...)`. En gratuit la fenêtre reste 30 jours ; les filtres naviguent cette liste sans débloquer ni simuler quoi que ce soit. Le commentaire d'en-tête du module documente explicitement ce choix.
- **Poids effectif** : la synthèse passe par `getEntryValue(entry, useWeights)` ; en gratuit la valeur reste le temps brut quel que soit le poids figé (test dédié : 10 min avec poids 10 restent 10). L'arrondi n'apparaît qu'à l'affichage (`Math.round` dans l'écran, `formatMetric`).
- **Note de plan calme** : affichée uniquement quand `!useWeights` (donc gratuit), textuelle, factuelle (« le poids effectif vaut 1… sans changer cette liste »), sans urgence fabriquée ni faux achat. L'eyebrow dynamique `${historyDays} jours` produit une sortie identique aux libellés précédents pour tous les plans actuels.
- **Ton non culpabilisant** : la répartition est descriptive par tâche ; aucune comparaison entre membres dans ce module (documenté et respecté). L'état vide est calme et actionnable (« Élargis la période ou affiche tout le foyer »).
- **Honnêteté des compteurs** : la liste tronquée à 20 lignes annonce « X premières sur Y entrées » au lieu du compteur antérieur trompeur ; les tâches dont la définition a disparu sont libellées « Tâche archivée » plutôt que silencieusement perdues.

## 3. Frontières de période (cible prioritaire de la directive)

Vérifié contre `src/domain/periods.ts` (préexistant, inchangé) : semaine = lundi 00:00 local (`startOfWeek`), mois = 1er 00:00 local (`startOfMonth`), inclusion `début <= completedAt <= now` — une entrée exactement à la borne de début est comptée une seule fois.

Les tests couvrent : dimanche 23:59 exclu / lundi 00:00 inclus (semaine), 31 juillet exclu / 1er août inclus (mois), filtre membre restrictif, sélection vide → synthèse zéro propre, déterminisme de tri à égalité (minutes ↓, collation fr, taskId) vérifié sur entrée inversée, et un test de composition bout-en-bout sur le semis de démo réel (19 entrées, celle à J−32 sort de la fenêtre 30 jours ; J−3 tombe dimanche 23 août et sort de la semaine ; 6 entrées en semaine, cohérence total/répartition vérifiée). J'ai recalculé ces attentes à la main depuis `demoData.ts` : elles sont exactes.

Deux trous de couverture subsistent face à la lettre de la directive (constat F2) : aucun test ne traverse un changement d'année (31 déc → 1er jan) pour `'month'`, et la borne haute `completedAt <= now` (entrée exactement à `now`, puis `now`+1 s) n'est pas épinglée.

## 4. Accessibilité

- Les deux contrôles utilisent le composant préexistant `SegmentedControl` : `accessibilityRole="radiogroup"`/`"radio"`, `accessibilityState={{ selected }}`, libellés explicites (« Période d'historique affichée », « Membre du foyer affiché »).
- `MetricCard` fusionne libellé, valeur et détail en un seul label accessible ; contraste de la note de plan (#264653 sur #F8F9FA) largement suffisant ; états vides en `Text` lisibles.
- Constat F1 (faible) : le filtre membre est le premier usage dynamique du contrôle et peut afficher jusqu'à 8 options ou plus (« Foyer » + membres ; Standard = 7 membres, Pro non borné) dans une seule rangée flex sans wrap ni défilement. À grande taille de texte sur petit écran, les libellés se compriment. Risque faible mais contraire à l'exigence canonique de lisibilité aux grandes tailles.

## 5. Sécurité, démo, isolation

- Aucun réseau : grep `fetch(|XMLHttpRequest|WebSocket|firebase` sur `src/` et `app/` ne retourne rien ; le diff n'ajoute aucun import réseau. La démo reste locale et synthétique.
- Aucun secret, aucune donnée personnelle, aucune journalisation ajoutée. Aucune logique serveur touchée : score, plan, rôle et heure de référence côté client restent cantonnés à la démo, et le docstring du module rappelle qu'en production l'heure de référence doit venir du serveur.
- Pas d'autorisation objet, concurrence, rejeu ou Stripe dans ce périmètre (aucun backend modifié) — non applicable, rien à régresser.
- Contenu hostile : les commentaires référencent uniquement des symboles réels (`startOfWeek`, `startOfMonth`, `getVisibleHistory`, `isEntryInPeriod`, `getEntryValue`) ; aucun motif externe fabriqué, aucun test ignoré, aucune instruction adressée à des agents dans le diff.

## 6. Checks réellement exécutés (worktree candidat)

1. `git -C /tmp/chorescore_mobile log --oneline de6470b..9d29981` → 1 commit ; merge-base = base acceptée.
2. `git -C /tmp/chorescore_mobile diff --stat de6470b..9d29981` → 3 fichiers, +451/−10 ; diff intégral relu.
3. `npm run check` → typecheck OK puis **78/78 tests** (71 précédents + 7 nouveaux), 0 échec, 0 ignoré.
4. `node --test .test-build/tests/history.test.js` → 7/7 nouveaux tests verts isolément.
5. `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` → export réussi, bundle ~2,8 Mo.
6. `npm audit --omit=dev --audit-level=high` → code sortie 0 ; 11 vulnérabilités modérées, **aucune haute/critique** ; lockfiles inchangés.
7. Greps ciblés : absence de réseau, absence de `.skip/.todo/.only/@ts-ignore/eslint-disable`.
8. Recoupement manuel des affirmations des tests contre `demoData.ts`, `periods.ts`, `scoring.ts`, `leaderboard.ts`.

Node v22.13.0, conforme à l'environnement imposé.

## 7. Constats

### MOB-C4-F1 (faible) — débordement possible du filtre membre à nombreuses options
`app/(tabs)/history.tsx` (usage ligne ~116) ; `src/components/SegmentedControl.tsx`. Premier usage dynamique du contrôle : jusqu'à 8+ segments dans une rangée unique sans wrap ; compression des libellés à grande taille de texte/petit écran. Correction minimale : wrap ou défilement horizontal au-delà de ~4 options, ou repli liste ; vérifier à la plus grande taille de texte système.

### MOB-C4-F2 (faible) — frontières année et borne haute non épinglées par test
`tests/history.test.ts`. La directive cible explicitement « années différentes » et les bornes exactes ; aucun test Décembre→Janvier pour `'month'`, aucune entrée exactement à `now` (incluse) vs `now`+1 s (exclue). Correction minimale : un cas avec `now` début janvier et entrées 31 déc 23:59 / 1er jan 00:00, plus une entrée exactement à `now`. Ces assertions doivent échouer si `getPeriodStart`/`isEntryInPeriod` régressent.

### MOB-C4-F3 (informationnel) — `new Date()` figé dans les mémos
`app/(tabs)/history.tsx` (~lignes 46-52). `filteredEntries`/`synthesis` mémorisent avec dépendances `[visibleEntries, periodFilter, memberFilter]` : si l'app reste ouverte à travers un changement de semaine/mois, le filtre garde l'ancienne période jusqu'à changement d'entrée. Convention préexistante (`visibleEntries`, `dailyPoints` identiques dans l'état accepté), impact démo seulement ; en production l'heure de référence vient du serveur. Correction optionnelle lors d'une passe rafraîchissement.

## 8. Décision

**Accepter.** Le candidat implémente réellement la tranche annoncée, sans placeholder, avec tests honnêtes qui démontrent ce qu'ils prétendent démontrer, sans régression de la démo hors ligne, sans dark pattern et sans élargissement de périmètre. Les trois constats sont non bloquants et transmis au directeur pour réponse ou programmation ; ils n'exigent ni correction immédiate du candidat ni retrait.
