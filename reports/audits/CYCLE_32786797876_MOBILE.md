# Audit mobile — cycle 32786797876 (round 1)

- **Rôle audité** : mobile (poste activé, critère assigné **DRC-02 — Persistance et reprise**)
- **Candidat** : branche `origin/cycle/chorescore/32786797876/mobile`, commit `83e8e3dda29c69fba15006215cd36b33a1c16da7` (« ChoreScore 32786797876: mobile snapshot ») — trouvé : **oui**
- **Base de comparaison** : `lab/chorescore` @ `b3d9eb6a2585aafaff7598d30291b923c6d0c489` (checkout courant, propre, branche acceptée intacte)
- **Décision** : **repair** (4 constats `mustFix: true`, 1 observation info)
- **Contrat machine** : `reports/audits/CYCLE_32786797876_MOBILE.json` (validé par script avant clôture)

## 1. Constat central : le candidat est un commit vide

Le snapshot candidat ne contient **aucun changement** par rapport à la branche
acceptée. Vérifié de trois manières indépendantes dans le worktree monté
`/tmp/chorescore_mobile` :

```
git rev-parse 83e8e3d^                  -> b3d9eb6a…  (= HEAD de lab/chorescore)
git diff b3d9eb6 83e8e3d --stat         -> (vide)
git diff b3d9eb6 83e8e3d --name-status  -> (vide)
git diff b3d9eb6 83e8e3d --raw          -> 0 ligne
git rev-parse b3d9eb6^{tree}            -> caaaa40e3da566510171c6982e1ca92ade3aa021
git rev-parse 83e8e3d^{tree}            -> caaaa40e3da566510171c6982e1ca92ade3aa021
```

Les deux commits pointent vers le **même arbre Git** : le candidat est octet
pour octet l'état accepté. Le worktree est propre vis-à-vis de son HEAD
(`node_modules` non suivi excepté), donc le workspace audit correspond bien au
snapshot poussé. Aucun contenu hostile n'était présent à obéir : il n'y a
simplement aucun contenu.

## 2. Ce que DRC-02 exige vs ce qui existe

`directives/TASKS.json` assigne au poste mobile le critère DRC-02 avec cinq
critères d'acceptation (hydratation explicite sans clignotement, sauvegarde des
changements métier sérialisée, migration/corruption sans perte silencieuse,
reprise ou expiration déterministe du chronomètre après redémarrage, tests
succès/migration/corruption/reprise). L'inspection du code réel montre qu'
**aucun n'est commencé** :

- `grep -r 'AsyncStorage|persist|hydrate|migration|schemaVersion|STORAGE_KEY' src/`
  → **zéro occurrence** ;
- `src/store/` ne contient que `AppProvider.tsx` et `appReducer.ts`, un store
  purement en mémoire : les données de démonstration disparaissent à chaque
  redémarrage ;
- `src/services/demoService.ts` ne gère `startedAt` qu'en session (le plafond
  de 24 h s'applique uniquement dans `stopEntry`, appelée en session) et
  `src/components/TaskRow.tsx` calcule l'écoulement via `Date.now()` pendant le
  montage seulement : aucune reprise après redémarrage ;
- `tests/` est identique à l'état accepté (11 fichiers) : aucun test de
  stockage, hydratation, migration, corruption ou reprise.

## 3. Checks réellement exécutés

| Check | Résultat |
| --- | --- |
| Identité SHA candidat / branche d'origine | égaux (`83e8e3d`) ; parent = base acceptée `b3d9eb6` |
| Diff intégral (`--stat`, `--name-status`, `--raw`) + égalité des arbres | vide / arbres identiques `caaaa40e` |
| Propreté du worktree candidat vs HEAD | propre (hors `node_modules` non suivi) |
| Recherche de contenu DRC-02 dans `src/` et lecture des fichiers concernés | aucune trace de persistance/hydratation/migration/reprise |
| `npm run check` (worktree candidat) | typecheck OK, **79/79 tests pass**, 0 échec — suite identique à l'état accepté |
| `npm audit --omit=dev --audit-level=high` | sortie 0, 11 vulnérabilités modérées, aucune haute/critique (identique à la base) |
| `sha256sum -c .github/immutable-files.sha256` | tous les fichiers OK |
| Dépendances et lockfiles | inchangés (conséquence du diff vide) |

Non exécutés, pour raison : l'export Android démo et les autres contrôles
applicatifs n'ont pas été relancés car les entrées sont octet pour octet celles
de l'état accepté déjà validé aux cycles précédents ; le diff vide rend leur
résultat identique par construction. Aucun check inventé, aucun résultat
supposé.

## 4. Constats

| ID | Gravité | mustFix | Objet |
| --- | --- | --- | --- |
| MOB-C5-F1 | high | **oui** | Aucune persistance versionnée ni hydratation explicite (acceptation DRC-02 items 1–2) |
| MOB-C5-F2 | high | **oui** | Aucune stratégie de migration ni de traitement de corruption (item 3) |
| MOB-C5-F3 | high | **oui** | Aucune reprise/expiration déterministe du chronomètre après redémarrage (item 4) |
| MOB-C5-F4 | high | **oui** | Aucun test succès/migration/corruption/reprise (item 5) |
| MOB-C5-F5 | info | non | Anomalie de processus : snapshot vide poussé au lieu d'un livrable ou d'un échec explicite |

Chaque constat `mustFix: true` doit être corrigé avant intégration et avant que
DRC-02 puisse être déclaré complet, quelle que soit sa gravité, conformément au
contrat de correction de `directives/AUDITOR.md`.

## 5. Décision : repair

- **Pas `accept`** : quatre constats `mustFix: true` ; la tranche assignée est
  entièrement absente.
- **Pas `reject`** : rien de dangereux ni de fondamentalement erroné — aucune
  régression, aucun secret, aucun réseau, aucun contournement ; le travail est
  simplement non livré et corrigeable par le même codeur.
- Round 1 : conformément à la directive, ce JSON `repair` retourne
  automatiquement au codeur mobile ; le candidat corrigé subira un second audit
  indépendant.

## 6. Constats hérités et périmètre

- Les constats mobiles hérités `MOB-C4-F1` et `MOB-C4-F2`
  (`mustFixBeforeRelease: true`) sont rattachés à **DRC-05**, critère non actif
  ce cycle : ils ne sont pas rejoués ici et restent conservés dans
  `docs/RELEASE_STATUS.json.openFindings`. `MOB-C4-F3` (info) suit le même
  sort.
- Le poste backend est désactivé ce cycle (`directives/TASKS.json`) : les
  constats `BE-C4-*` sont hors périmètre de cet audit.
