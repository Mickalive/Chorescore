# Audit indépendant — candidat MOBILE, cycle 32857952394 (round 1)

- Rôle : auditeur indépendant de livraison (`governance/roles/INDEPENDENT_RELEASE_AUDITOR.md`)
- Critère assigné : **DRC-02 — Persistance et reprise** (`directives/TASKS.json`, backend désactivé)
- Base acceptée : `2827835` (lab/chorescore, checkout courant intact)
- Candidat : `9d47082d33517a50c33ade9affb65bf61c60bd4e` — branche
  `origin/cycle/chorescore/32857952394/mobile`, worktree `/tmp/chorescore_mobile`
- Décision : **accept** — aucun constat `mustFix: true`

## 1. Identité et périmètre du candidat

Le worktree candidat est sur un HEAD détaché identique au commit pointé par la
branche candidate (`git rev-parse` recoupés). Le merge-base avec la branche
acceptée est exactement `2827835` : un seul commit candidat, pas d'historique
réécrit.

Diff réel (`git diff --stat 2827835 9d47082`) : 10 fichiers, +1222/−26,
strictement dans le périmètre mobile autorisé :

| Fichier | Changement |
| --- | --- |
| `src/store/persistence.ts` | nouveau, 548 lignes : sérialisation, validation, migration, reprise, file d'écriture |
| `tests/persistence.test.ts` | nouveau, 454 lignes, 17 tests |
| `src/store/AppProvider.tsx` | hydratation explicite + effet de persistance séquencée |
| `src/store/appReducer.ts` | état `hydrated`, action `HYDRATE` |
| `src/store/demoStorage.ts` | nouveau adaptateur AsyncStorage local |
| `app/_layout.tsx` | porte `HydrationGate` avant tout rendu métier |
| `src/components/TaskRow.tsx` | chrono recalculé depuis `startedAt`, plafond 24 h affiché |
| `app/(tabs)/profile.tsx`, `app/onboarding.tsx`, `src/components/DemoBanner.tsx` | textes mis en cohérence avec la persistance locale |

Aucun fichier interdit touché (grep négatif sur `governance/`, `.github/`,
`.opencode/`, `directives/`, `MAIN_PROMPT.md`, `AGENTS.md`, `opencode.json`,
`package.json`, `package-lock.json`, `functions/`). Aucune dépendance ajoutée :
`@react-native-async-storage/async-storage` 2.2.0 était déjà une dépendance
figée de la base acceptée. Le manifeste immuable vérifié dans le worktree
candidat (`bash .github/scripts/verify-immutable-governance.sh`) : 19/19 OK,
sortie 0.

## 2. Vérification du critère DRC-02, point par point

1. **Hydratation explicite sans clignotement** — `HydrationGate`
   (`app/_layout.tsx`) ne rend aucun écran tant que `state.hydrated` est faux ;
   écran de chargement annoncé (`accessibilityLiveRegion="polite"`,
   libellé « Préparation de la démonstration… »). Le semis fictif ne sert qu'au
   premier lancement ou en récupération. Testé (« premier lancement », « relance »).
2. **Sauvegarde des changements métier, sérialisation stable** —
   `buildPersistedDocument` reconstruit chaque entité champ par champ en ordre
   fixe ; champs transitoires (avis, paywall, compteur analytics) exclus, testé
   par comparaison d'octets. L'effet de persistance ne déclenche qu'après
   `hydrated`, saute les payloads identiques (`lastPayloadRef`) et liste
   précisément les champs persistés en dépendances.
3. **Migration et corruption sans perte silencieuse** — migration v1→v2 prudente
   (consentement dérivé de l'onboarding réellement accompli, `analyticsOptIn`
   jamais fabriqué à `true`, `acceptedAt` honnêtement `null`) ; version future
   refusée (`schema-future`) ; 12 formes invalides mutées une par une sont
   refusées ; valeur corrompue ou stockage illisible → récupération sur le semis
   avec avis visible, puis écrasement volontaire au prochain enregistrement.
4. **Reprise/expiration déterministe du chrono** — `resolveResumedTimers`
   recalcule depuis `startedAt` persisté avec horloge injectée ; bornes 24 h
   testées exactement (−1 s, exact, +3 jours) ; horloge décalée dans le futur
   ramenée à 0 ; avis de relance nommant le chrono et son temps écoulé. Cohérence
   bout-en-bout vérifiée : `MAX_TIMER_DURATION_SECONDS` (persistence) =
   plafond `Math.min(24*60*60, …)` préexistant de `demoService.completeTimer`,
   et affichage `TaskRow` plafonné à la même borne avec note explicite à
   expiration. Un test complet restaure, reprend, termine via un service neuf et
   retrouve durée 1800 s et score 60.
5. **Tests succès/migration/corruption/reprise** — 17 nouveaux tests couvrant
   précisément ces familles, plus file d'écriture (séquencement sans
   chevauchement, échec propagé sans bloquer les suivants).

Le wiring React est sain : initialisation paresseuse par refs stables, effet
d'hydratation gardé contre les démontages (`cancelled`), échec d'écriture annoncé
honnêtement (« Les modifications n'ont pas pu être enregistrées… ») avec remise
à zéro du cache de payload donc nouvelle tentative à la mutation suivante.
`RESET_DEMO` traverse l'effet de persistance : la réinitialisation survit désormais
au redémarrage. Premier lancement : rien n'est écrit avant la première mutation,
l'état « first-launch » reste naturel. Les textes modifiés suppriment des
affirmations devenues fausses (« rien n'est conservé », « sans persistance ») :
honnêteté de démo améliorée, aucune promesse réseau ou d'achat simulé introduite.

## 3. Contrôles déterministes exécutés (worktree candidat, Node v22.13.0)

- `npm run check` : typecheck OK puis **96/96 tests** pass, 0 échec
  (79 appels `test(` statiques côté base acceptée + 17 nouveaux).
- `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` :
  export réussi, bundle Android ~2,8 Mo, démo hors ligne intacte.
- `npm audit --omit=dev --audit-level=high` : sortie 0, 11 vulnérabilités
  modérées, aucune haute/critique ; parité avec la base acceptée (sortie 0) ;
  lockfiles inchangés.
- Scan du diff `fetch(|XMLHttpRequest|WebSocket|http(s)|secret|token|password|EXPO_PUBLIC` :
  zéro occurrence — aucune requête réseau, aucun secret, aucune variable publique.
- Scan `.skip|.only|.todo|@ts-ignore|eslint-disable` sur les fichiers nouveaux :
  zéro — aucun test ni contrôle affaibli.
- Sondes adversariales maison contre les modules compilés : entrée `taskId`
  fantôme et ids dupliqués acceptés par le validateur (constats F1/F2 ci-dessous,
  replis gracieux confirmés dans `src/domain/history.ts` « Tâche archivée ») ;
  horloge future écrêtée à 0 ; formes corrompues (`null`, `"str"`, `42`, `{}`,
  `schemaVersion` 99 ou 0) toutes récupérées avec `hydrated: true` ; document v1
  au snapshot invalide rejeté après migration ; état à 20 000 entrées sérialisé
  sans exception (~5,3 Mo). Note d'honnêteté : ma première sonde « données
  excessives » échouait à cause d'un bug de mon propre harnais (document persisté
  passé comme état applicatif) ; corrigé, le comportement du candidat est correct.

## 4. Constats (aucun bloquant)

- **MOB-CYCLE32857952394-F1 (low)** — le validateur ne contrôle pas l'intégrité
  référentielle (`taskId`/`userId` inconnus tolérés). Replis gracieux réels :
  libellé « Tâche archivée », classement ignorant les utilisateurs inconnus,
  chrono fantôme jamais appareillé. Durcissement facultatif.
- **MOB-CYCLE32857952394-F2 (low)** — pas d'unicité des identifiants à la
  lecture ; des doublons ne peuvent provenir que d'un fichier façonné à la main
  et fusionnent sans crash à l'affichage.
- **MOB-CYCLE32857952394-F3 (info)** — pas de borne applicative de taille du
  document ; au-delà des limites AsyncStorage l'écriture échoue de façon visible
  (avis + retry), ce qui reste honnête pour la démo.
- **MOB-CYCLE32857952394-F4 (info)** — `HydrationGate` patienterait indéfiniment
  si l'adaptateur ne se résolvait jamais ; `readStoredState` capture les rejets
  mais pas une promesse éternellement en attente. Théorique avec AsyncStorage.

Aucun de ces constats n'exige de correction avant intégration ni pour compléter
DRC-02 : tous sont `mustFix: false`.

## 5. Décision

**accept.** La tranche est réelle, testée, dans le périmètre, honnête et sans
régression détectée. Les quatre constats sont transmis comme observations
facultatives (durcissement possible sous DRC-05 ou en passe robustesse), sans
obligation. Le contrat machine est `reports/audits/CYCLE_32857952394_MOBILE.json`.
