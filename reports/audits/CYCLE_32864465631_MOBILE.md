# Audit mobile — cycle 32864465631 (round 1)

- **Poste** : auditeur indépendant de livraison (`governance/roles/INDEPENDENT_RELEASE_AUDITOR.md`)
- **Candidat** : `origin/cycle/chorescore/32864465631/mobile` = `1fce8b3454da6cb629e3e637cd242b68db93de77`, branche trouvée = true
- **Base acceptée** : `lab/chorescore` @ `71c0d5d` (checkout courant, arbre propre)
- **Critère assigné** : DRC-03 — Correction et contrôle des données
- **Décision** : **accept** (0 constat `mustFix: true`)
- **Contrat machine** : `reports/audits/CYCLE_32864465631_MOBILE.json`

## 1. Identité et périmètre du candidat

Le worktree `/tmp/chorescore_mobile` est en HEAD détaché sur `1fce8b3`, identique à
`origin/cycle/chorescore/32864465631/mobile`. Son parent unique est `71c0d5d`, HEAD exact de la
branche acceptée : un seul commit candidat, pas d'historique réécrit.

Diff réel (`git diff 71c0d5d..1fce8b3`) : 12 fichiers, +1439/−16, strictement dans le périmètre
mobile autorisé :

| Fichier | Changement |
| --- | --- |
| `src/store/appReducer.ts` | actions `UPDATE_TASK`, `ARCHIVE_TASK`, `EDIT_ENTRY`, `DELETE_ENTRY`, `CANCEL_TIMER` + planificateurs `planUpdateTask`, `planArchiveTask`, `planEditEntryDuration`, `planDeleteEntry`, `planCancelTimer` ; refus de chrono/saisie sur tâche archivée |
| `src/store/AppProvider.tsx` | exposition `updateTask`, `archiveTask`, `cancelTimer`, `editEntryDuration`, `deleteEntry` (schéma plan → service → dispatch) |
| `src/store/persistence.ts` | validateur renforcé : unicité par collection + intégrité référentielle des entrées |
| `src/services/appService.ts` / `demoService.ts` / `productionService.ts` | interface étendue ; démo implémente `updateTask` et `editCompletedEntryDuration` ; production échoue fermé |
| `src/components/EntryCorrectionModal.tsx` (nouveau) | modale de correction avec aperçu live depuis le poids figé |
| `src/components/TaskFormModal.tsx` / `TaskRow.tsx` | mode édition, boutons Modifier/Archiver, Annuler un chrono actif |
| `app/(tabs)/index.tsx` / `history.tsx` | confirmations Alert, actions sur entrées du membre actif, note de tâches archivées |
| `tests/data-control.test.ts` (nouveau) | 19 tests |

Aucun changement de dépendance, lockfile, `functions/`, gouvernance ou workflows
(`git diff … -- package.json package-lock.json functions` vide).

## 2. Examen hostile

- Scan du diff pour instructions cachées, réseau, secrets, exécution dynamique :
  zéro occurrence (`fetch(`, `http(s)://`, `WebSocket`, `eval(`, `child_process`,
  « ignore previous », etc.). Les commentaires du diff sont techniques et exacts.
- Aucun test affaibli : aucun `.skip`/`.only` introduit, les suites préexistantes passent intactes.
- Aucun placeholder présenté comme terminé : chaque bouton ajouté est câblé à une mutation réelle.
- Manifeste immuable vérifié dans le worktree candidat : `sha256sum -c .github/immutable-files.sha256`
  → toutes les empreintes OK.

## 3. Vérification du comportement DRC-03

Toutes les opérations suivent le schéma établi (plan pur → service → reducer → effet de
sauvegarde global), donc aucune mutation de contrôle ne contourne la persistance :

1. **Modification de tâche** — `UPDATE_TASK` ne touche que `tasks` ; le test asserte
   `deepEqual(next.entries, entriesBefore)` : aucun `weightSnapshot` ni score réécrit. En gratuit,
   le poids soumis est ignoré et le poids existant conservé (champ verrouillé en UI, paywall).
2. **Archivage** — confirmé par Alert ; `active: false` conserve la définition, l'historique reste
   libellé (testé via `buildHistorySynthesis`) ; `planStartTimer`/`planManualEntry` refusent une
   tâche archivée ; double archivage refusé ; note de comptage affichée sur l'écran Tâches.
3. **Correction d'une entrée terminée** — le service recalcule `(durée/60) × weightSnapshot`
   uniquement : il ne reçoit structurellement ni la tâche ni son poids courant. Test clé : poids
   courant passé de 2 à 9 après validation, correction à 50 min → score 100 (= 50×2, pas 450).
   `startedAt`/`completedAt`/`periodKey`/`isManual` préservés. Refus : durée invalide (0, 12.5,
   1441), entrée inconnue, d'un autre membre ou encore en cours.
4. **Suppression confirmée** — filtrage réel des entrées ; tests prouvant l'absence d'orpheline
   dans classement (delta exact de score/compteur), historique visible, synthèse et document
   persisté relu ; état vide après suppression totale couvert.
5. **Annulation de chrono** — l'entrée en cours disparaît sans fantôme ; `applyRestartRules` à
   J+2 produit zéro événement et un snapshot identique ; `planCompleteTimer` refuse ensuite ;
   refus aussi pour entrée inconnue, terminée ou d'un autre membre.
6. **Validateur de persistance (constats hérités F1/F2)** — `isDurableState` refuse désormais tout
   `taskId`/`userId` inexistant et tout identifiant dupliqué (users, tasks, entries, et paire
   naturelle foyer+membre pour les adhésions, qui n'ont pas d'id). Une charge invalide part en
   quarantaine avec le parcours « corruption » existant, jamais de perte silencieuse.
   `MOB-CYCLE32857952394-F1/F2` sont résolus avec preuve.
7. **Frontière production** — `ProductionAppService` échoue fermé (`ProductionModeDisabledError`)
   sur les deux nouvelles opérations, testé.

## 4. Preuve de mutation (rouge si régression)

En copie jetable `/tmp/opencode/mutation_check` (le candidat n'a jamais été modifié) : retrait du
bloc d'invariants référentiels d'`isDurableState` puis relance de la suite → exactement les trois
nouveaux tests du validateur échouent (16 pass / 3 fail). Les tests détectent bien une régression
de la validation. Copie supprimée ensuite.

## 5. Contrôles déterministes exécutés (worktree candidat)

| Check | Résultat |
| --- | --- |
| `npm ci --ignore-scripts` | OK |
| `npm run check` | typecheck OK, **121/121 tests** (102 préexistants + 19 nouveaux), 0 échec |
| `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` | export OK, bundle ~2,8 Mo, démo hors ligne intacte |
| `npm audit --omit=dev --audit-level=high` | sortie 0 (11 vulnérabilités modérées, aucune haute/critique ; parité avec la base acceptée) |
| Manifeste immuable | toutes empreintes OK |

## 6. Constats

Trois constats, tous `severity: info` et `mustFix: false` (aucune correction obligatoire) :

1. **MOB-CYCLE32864465631-F1** — les confirmations UI (`Alert.alert`) ne sont pas couvertes par un
   test automatisé ; les couches domaine/service/persistence le sont. Conforme à la stratégie de
   test actuelle du dépôt (pas de harnais UI) ; à reprendre si un harnais apparaît (DRC-05).
2. **MOB-CYCLE32864465631-F2** — en gratuit, l'édition conserve le poids antérieur au lieu de
   ramener le champ à 1. Non contraire au canon (poids effectif des nouvelles entrées = 1, champ
   verrouillé), comportement délibéré, documenté et testé.
3. **MOB-CYCLE32864465631-F3** — le champ de correction accepte jusqu'à 4 chiffres (> 1440) mais
   toute valeur hors borne est refusée avec message explicite et aperçu en attente ; le refus
   explicite est plus honnête qu'un clamp silencieux.

## 7. Décision

**accept** — conformément au contrat : décision `accept` si et seulement si aucun constat n'a
`mustFix: true`. Le candidat satisfait tous les points d'acceptation DRC-03 de
`directives/TASKS.json` avec des preuves déterministes ; le directeur peut l'intégrer et
consigner la résolution de `MOB-CYCLE32857952394-F1/F2`.
