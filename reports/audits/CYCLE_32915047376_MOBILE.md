# Audit mobile — cycle 32915047376 (round 1)

- **Rôle** : auditeur indépendant de livraison (`governance/roles/INDEPENDENT_RELEASE_AUDITOR.md`)
- **Critère assigné** : DRC-04 — Fonctions premium honnêtes en démo (`directives/TASKS.json`, `governance/RELEASE_DEFINITION.json`)
- **Candidat** : commit `aecbb48` (« ChoreScore 32915047376: merge mobile candidate slices »), workspace `/tmp/chorescore_mobile`
- **Base de comparaison** : branche acceptée `lab/chorescore` à `ff09f36` — le parent unique du candidat est exactement ce commit (parenté vérifiée par `git cat-file -p`) ; le diff est donc le travail complet du cycle.
- **Décision : `accept`** — zéro constat `mustFix: true`.

## Périmètre réel du diff

12 fichiers, +1603/−29, tous dans la surface autorisée du poste mobile
(`app/`, `src/`, `tests/`) ; aucun fichier immuable, aucune dépendance, aucun
lockfile touché (`git diff --name-only | grep -vE '^(app/|src/|tests/)'` → 0).
Le diff a été lu intégralement. Aucune instruction cachée, aucun appel réseau,
aucun secret, aucun affaiblissement de test détecté (recherche ciblée sur
`ignore|instruction|curl|fetch|http|secret|token|api_key` dans le diff : seul
le champ légitime `errorAnnouncement.token` apparaît).

## Vérifications réellement exécutées

1. **Typecheck candidat** : `npm run typecheck` → OK.
2. **Tests ciblés** (les contrôles larges — `npm run check` complet, export
   Android démo — relèvent du préflight déterministe parallèle, non dupliqués) :
   `node --test` sur `premium.test.js`, `persistence.test.js`,
   `history.test.js`, `periods.test.js` → **57/57 pass**, dont les 23 nouveaux
   tests `premium.test.ts`.
3. **Preuve de mutation** sur l'invariant le plus sensible (isolation
   inter-foyers) : retrait de la garde `entry.householdId !== householdId`
   dans `isHouseholdStore`, recompilation `tsc -p tsconfig.test.json`
   (première tentative sans recompilation — résultat invalide, corrigé), puis
   relance → test 46 « chaque magasin porte cohéremment l'identité de son
   propre foyer » échoue. Fichier restauré depuis copie, arbre redevient
   identique au candidat (`git status` propre), tests à nouveau 46/46.
   Les tests du validateur mordent réellement.

## Analyse par critère d'acceptation DRC-04

### 1. Filtres semaine/mois : synthèses réelles, méthode et bornes visibles

`describePeriodBounds` (nouveau, `src/domain/periods.ts`) produit des bornes
déterministes strictement alignées sur `getPeriodStart` (semaine lundi 00:00
locale +6 jours ; mois civil). Le libellé est affiché dans l'historique
(`methodNote` avec méthode : « temps brut (poids effectif 1) » ou « durée ×
poids convenu ») et repris tel quel dans le rapport exporté. Tests incluant le
passage d'année (1er janvier 2027 → semaine « décembre 2026 / janvier 2027 »).
Les filtres restent gratuits et ne déclenchent aucun paywall (comportement
existant préservé, commentaire `history.ts` inchangé).

### 2. Foyers locaux multiples réels et isolés

`CREATE_HOUSEHOLD`/`SWITCH_HOUSEHOLD` échangent des magasins complets
(`HouseholdStore`) : aucune donnée ne franchit la frontière. Test
« allers-retours complets » prouvant l'absence de fuite dans les deux sens.
Le validateur de persistance est étendu aux nouveaux champs : validation
stricte de chaque magasin (unicité users/tasks/entries interne, intégrité
référentielle interne, `currentUserId` connu, adhésions et entrées rattachées
au foyer du magasin), unicité globale des identifiants de foyer (avec le foyer
actif), des clés d'adhésion et des identifiants d'entrée entre magasins. Champ
**additif** : un document écrit avant la fonction reste lisible (test dédié),
donc pas de quarantaine en cascade pour les installations existantes — choix
de migration correct. Les nouvelles tâches/entrées portent bien
`state.household.id` (câblage préexistant lignes 265/319/370 de
`AppProvider.tsx`, non régressé) : pas de risque de refus validateur en usage
normal. Les identifiants d'entrée sont `${prefix}_${ms}_${seq}` avec compteur
de module monotone : collision inter-magasins irréaliste. Un chrono actif
vit dans les entrées de son foyer et bascule atomiquement avec lui ; la reprise
par `startedAt` (règles DRC-02) n'est pas affectée.

### 3. Export local réellement consultable

L'ancienne alerte « Rapport simulé » est remplacée par un vrai rapport texte
(`buildExportReport`) calculé depuis la sélection affichée (mêmes entrées,
même méthode), prévisualisé intégralement (`ReportPreviewModal`, texte
sélectionnable et défilant) puis partageable via le partage système
(`Share.share`). Aucun réseau, aucun compte ; échec de partage annoncé
honnêtement, annulation silencieuse ; mentions explicites « données fictives
… aucune synchronisation réseau ». Sélection vide traitée honnêtement (test).
En gratuit, le geste ouvre le paywall `export_pdf` (fonction premium selon le
canon) sans jamais simuler de contournement. La fenêtre « tout l'historique
visible » est honnête : `getVisibleHistory` borne déjà la liste affichée.

### 4. Pondération : poids effectif 1 en gratuit, snapshot figé

Aucune réécriture de score historique dans le diff ; `getEntryValue` et
`weightSnapshot` inchangés. Test dédié : rapport gratuit en temps brut,
jamais de points. Les corrections d'entrée continuent de recalculer depuis le
snapshot figé (code DRC-03 intact).

### 5. Paywall honnête

Boutons renommés « Activer Standard/Pro (démo) » avec libellés accessibles ;
le texte dit explicitement qu'aucun achat n'est possible et que seuls le
scénario fictif change — plus honnête qu'avant. Portes premium vérifiées deux
fois (contexte + planificateur), paywall calme au moment du geste.

### 6. États vides, erreurs, confirmations, accessibilité

Formulaire de création : champ labellisé, focus gardé par miroir `onShow`,
erreurs inline avec jeton d'annonce croissant (`computeErrorAnnouncement`),
live region Android + annonce impérative iOS, `KeyboardAvoidingView` +
`ScrollView` (petits écrans / grandes tailles). Réinitialisation désormais
confirmée (Alert destructive) car elle efface aussi les foyers créés — test
dédié. Contraste vérifié : note de méthode #264653 sur #F1FAEE (~10:1),
erreur #A9422F sur #FFF4F1 (~5,6:1) — AA OK. Plafond local de 5 foyers
annoncé dans le message de refus (limite réelle, jamais présentée comme un
paywall).

### 7. Non-régressions et constats hérités

MOB-CYCLE32857952394-F1/F2 : rejoués via les tests du validateur (intégrité
référentielle, unicité) — toujours verts, y compris après extension
multi-magasins. Les constats DRC-05/DRC-07 restent hors périmètre de ce round
(critère actif DRC-04 uniquement). `RESET_DEMO` vide bien `otherHouseholds`.

## Constats non bloquants (informationnels)

- **N1 (info)** : `describePeriodBounds(periodFilter, new Date())` reconstruit
  des formateurs `Intl` à chaque rendu de l'historique. Impact négligeable,
  aucune action requise.
- **N2 (info)** : `CreateHouseholdResult` porte une chaîne d'erreur vide sur
  le chemin paywall ; forme d'API interne seulement, l'appelant la gère
  correctement.

## Conclusion

Chaque critère d'acceptation DRC-04 est satisfait par un comportement réel,
testé (succès, refus, états vides), sans placeholder ni fausse promesse, sans
réseau ni dépendance nouvelle, avec migration de document additive et
validateur étendu effectif (preuve de mutation). Aucun constat
`mustFix: true` : décision **accept**, intégration possible par le directeur.
