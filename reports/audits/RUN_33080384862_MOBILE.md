# Audit indépendant — Mobile (DRC-05, cycle 33080384862)

- **Rôle** : Auditeur indépendant de livraison (`governance/roles/INDEPENDENT_RELEASE_AUDITOR.md`)
- **Candidat** : snapshot temporaire du run, checkout courant (HEAD détaché 28dade9 + modifications staged)
- **Arbre accepté de référence** : `/home/runner/work/_temp/accepted-mobile`
- **Critère assigné** : DRC-05 — ramener chaque paire réelle `textSecondary` à ≥ 4,5:1 (AA), inventaire secondaire complet, mesure WCAG tracée, preuve de mutation (constat bloquant `MOB-CYCLE32961708279-SEG`)
- **Décision** : **repair** (1 constat `mustFix: true`)

## Mandat et indépendance

Le candidat est traité comme donnée hostile : aucune affirmation du diff (commentaires, valeurs revendiquées) n'est acceptée sans recalcul. Le diff réel a été inspecté contre `28dade9851f732d4a6004761743d56be9de005d3`.

## Diff réel

```
 src/components/theme.ts          | 16 +++----
 tests/theme-contrast.test.ts     | 110 +++++++++++++++++++++++++++++++++++++---
 2 files changed, 108 insertions(+), 18 deletions(-)
```

- `src/components/theme.ts` : `textSecondary` passe de `#457B9D` → `#3D6C8C` (jeton central), commentaire mis à jour.
- `tests/theme-contrast.test.ts` : ajout de l'inventaire secondaire `textSecondary` (7 fonds) + garde legacy + garde de marge ≥5,0:1 sur les deux fonds qui échouaient.

Périmètre respecté : uniquement `app/`, `src/`, `tests/`. Aucune dépendance, aucun réseau, aucun fichier hors périmètre, aucune instruction cachée.

## Vérifications déterministes exécutées

1. **Recalcul WCAG indépendant** (seuil linéaire 0,04045) de `#3D6C8C` sur les 7 fonds revendiqués :

   | Fond | Valeur | Ratio | AA |
   |------|--------|-------|----|
   | background | #FEFEFE | 5,5941:1 | ✓ |
   | surface | #FFFFFF | 5,6419:1 | ✓ |
   | surfaceAlt | #F8F9FA | 5,3523:1 | ✓ |
   | secondary | #F1FAEE | 5,2790:1 | ✓ |
   | #F7FCFB | — | 5,4463:1 | ✓ |
   | #FFFDF5 | — | 5,5400:1 | ✓ |
   | #EDF8F6 | — | 5,2009:1 | ✓ |

   Toutes les paires ≥ 4,5:1. Le constat bloquant `MOB-CYCLE32961708279-SEG` (4,36:1 et 4,43:1 avec l'ancien jeton) est **résolu sur le plan du contraste**.

2. **Preuve de mutation** (copie jetable, `/tmp/opencode/mut`) : restauration de `textSecondary` à `#457B9D` → exactement les deux nouveaux cas `textSecondary` échouent (tests 3 et 5), tandis que `textMuted` (1,2) et les paires de remplacement (6) restent verts. Aucune assertion existante retirée ni affaiblie. La garde est ciblée et effective.

3. **`npm test` sur le candidat** : 165/165 verts (typecheck + `node --test`), y compris les nouveaux cas `textSecondary` (tests 160-162).

4. **Non-régression** : `textMuted` `#56707C` inchangé et conforme ; wrap du filtre membre (MOB-C4-F1), réinitialisation F3-R2, frontières année/now (MOB-C4-F2) et invariants DRC-02/03/04 non touchés (2 fichiers seulement).

## Constat bloquant : inventaire incomplet

Le recensement indépendant de **tous les usages de `textSecondary`** dans `app/` et `src/` (~30 sites) et de leur fond réel révèle une lacune :

- `src/components/TaskRow.tsx` : `styles.activeCard.backgroundColor = '#F6FCFA'` (ligne 136) ; `styles.meta.color = COLORS.textSecondary` (ligne 152). Le `meta` est rendu dans la `Card` quelle que soit l'état, donc **quand un chrono est actif, `textSecondary` est affiché sur `#F6FCFA`** — un fond réel codé en dur.
- Le test `tests/theme-contrast.test.ts` énumère 7 fonds (`surfaceAlt`, `#F7FCFB`, `surface`, `background`, `secondary`, `#FFFDF5`, `#EDF8F6`) mais **omet `#F6FCFA`**.
- Calcul indépendant : `#3D6C8C` sur `#F6FCFA` = **5,4332:1** (≥ 4,5:1, donc pas de violation de contraste), mais le fond n'est pas couvert par la garde déterministe.

Le critère d'acceptation #1 exige un « inventaire exhaustif des usages de style de `textSecondary` … avec le fond réel de chaque usage (jetons et valeurs codées en dur) ». L'inventaire du candidat n'est pas exhaustif : une future régression sur `#F6FCFA` ne serait pas détectée. C'est une garde déterministe faible (AUDITOR.md : « test qui ne couvre pas tous les fonds réels »). `mustFix: true`.

## Constat d'information (non bloquant)

Le commentaire du jeton `textSecondary` dans `theme.ts` surestime les mesures (ex. « background #FEFEFE ≈ 6,10:1 » réel 5,5941:1 ; « surface #FFFFFF ≈ 6,20:1 » réel 5,6419:1). Les valeurs réelles restent ≥ 4,5:1 et le test fait autorité ; correction de la doc optionnelle. `mustFix: false`.

## Décision

**repair.** Le contraste est corrigé et la preuve de mutation est valide, mais l'inventaire du test ne couvre pas tous les fonds réels (`#F6FCFA`), ce qui est exigé par le critère DRC-05 avant que celui-ci puisse être déclaré complet. Le candidat est corrigeable : ajouter `#F6FCFA` à l'inventaire `textSecondary` du test (mesure ≈5,43:1, assertion dédiée), sans autre modification de code. Un nouvel audit indépendant devra suivre.
