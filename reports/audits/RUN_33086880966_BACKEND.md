# Audit backend — cycle 33086880966

## Résumé

Le candidat DRC-07 modifie **uniquement** `docs/security/README.md` (+4 lignes, -2 lignes,
1 fichier). La modification met à jour le numéro de cycle de 33080384862 à 33086880966
et ajoute une clarification que le décompte de tests a été rejoué intégralement à ce
cycle. Aucun code, aucune dépendance, aucun lockfile, aucun fichier hors `docs/security/**`
n'est touché.

## Vérifications exécutées

### 1. Périmètre du diff

- `git diff af52f9f75d5cdddf23e5e20b368356c1e6ae9b86 --name-only` retourne uniquement
  `docs/security/README.md` (1 fichier).
- Aucune modification dans `app/`, `src/`, `tests/`, `functions/`, `package.json`,
  `package-lock.json`, `functions/package.json`, `functions/package-lock.json`, `.github/`.
- Verdict : périmètre strictement `docs/security/**`.

### 2. Contenu du diff

Le diff réel (2 hunks) :
1. Ligne 150 : `## Contrôles exécutés lors du cycle 33080384862` → `…33086880966`
2. Lignes 153-154 : ajout de « ; le décompte ci-dessous a été rejoué intégralement à
   ce cycle, pas recopié du cycle 33080384862 ».

Aucune autre modification.

### 3. Vérification du contenu documentaire

| Claim du document | Vérification indépendante | Résultat |
|---|---|---|
| 106/106 tests unitaires purs | `npm --prefix functions run check` : 106 pass, 0 fail | ✅ |
| observedCaller = extracteur pur, sans SDK | `functions/src/observedCaller.ts` n'importe ni Firestore, ni Admin SDK, ni firebase-functions | ✅ |
| email_verified comparé strictement à `true` | Ligne 57 : `request.auth?.token?.email_verified === true` | ✅ |
| 2 call sites `caller: observedCaller(request)` dans invites.ts | `grep -c` : 2 (lignes 204, 304) | ✅ |
| 1 call site dans tasks.ts | `grep -c` : 1 (ligne 308) | ✅ |
| Épinglage source dans observedCallerWiring.test.ts | Fichier existant (107 lignes), WIRING_PATTERN + IDENTITY_LITERAL_PATTERN | ✅ |
| Refus cross-foyer « Accès au foyer refusé. » | Présent dans taskCompletion.test.ts (lignes 119, 157, 233, 437) | ✅ |
| Refus cross-user « Cette tâche appartient à un autre membre. » | Présent dans taskCompletion.test.ts (lignes 244, 465) | ✅ |
| Versions deps : firebase-admin 14.3.0, firebase-functions 7.3.2, stripe 22.5.0 | `functions/package.json` | ✅ |
| STRIPE_ENABLED, STRIPE_LIVE_MODE, ANALYTICS_AGGREGATION_ENABLED = false | `functions/src/config.ts` : default: false pour les trois | ✅ |
| npm audit non exécuté dans le bac à sable | Déclaration honnête ; l'auditeur l'exécute indépendamment | ✅ |
| Aucun appel réseau, aucun secret, aucune donnée personnelle | Tests uniquement en logique pure, whsec_local_unit_test_only factice | ✅ |

### 4. Audit de dépendances

`npm --prefix functions audit --omit=dev --audit-level=high` : **7 vulnérabilités modérées
préexistantes** (uuid → gaxios → teeny-request → @google-cloud/storage → firebase-admin →
firebase-functions), aucune nouvelle introduction par ce candidat. Aucune vulnérabilité
haute ou critique. Le fix require `firebase-admin@10.3.0` (breaking change), hors
périmètre de ce cycle.

### 5. Limites documentaires restantes

Le document liste honnêtement 8 contrôles encore bloqués (handlers non bout en bout sans
émulateur, épinglage textuel non comportemental, règles Firestore non testées, webhook
Stripe non bout en bout, concurrence/rétention/TTL, revue humaine, test d'intrusion,
Firebase/Stripe/analytics/déploiement désactivés). Aucun n'est présenté comme résolu.
Aucune affirmation de sécurité absolue.

## Constat

Le candidat est une mise à jour triviale du numéro de cycle et une clarification de
traçabilité. Toutes les affirmations du document ont été vérifiées indépendamment et
sont fidèles au code accepté. Le périmètre est strict, aucun code n'est touché, aucun
constat `mustFix` n'est identifié.

**Décision : accept**
