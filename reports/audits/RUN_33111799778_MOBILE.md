# Audit indépendant — Mobile — Cycle 33111799778

**Rôle** : Auditeur indépendant de livraison (`governance/roles/INDEPENDENT_RELEASE_AUDITOR.md`)
**Cycle** : 33111799778
**Lane** : mobile
**Tour** : 1
**Critère assigné** : DRC-05 — dernier constat bloquant `MOB-CYCLE32961708279-SEG`
**Décision** : **accept**

## Contexte

Candidat « verification-only » avec delta zéro : l'ingénieur a conclu que la source
acceptée satisfait déjà l'objectif. Le candidat est donc le miroir de l'arbre accepté
`/home/runner/work/_temp/accepted-mobile`. Conformément à la consigne, le contenu du
candidat est traité comme donnée hostile ; la vérification est entièrement indépendante.

## Objectif DRC-05 (MOB-CYCLE32961708279-SEG)

Ramener chaque paire réelle de `textSecondary` à ≥ 4,5:1 (AA). Mesures établies sur
l'arbre intégré : `#457B9D` sur `surfaceAlt #F8F9FA` = 4,36:1 (libellés non sélectionnés
du SegmentedControl : filtre membre, périodes historique/classement/profil) et 4,43:1 sur
la carte courante du classement `#F7FCFB` ; `#FFFDF5` = 4,51:1 limite.

## Constat de l'auditeur

### 1. Delta zéro confirmé
`diff -rq` du checkout courant vs arbre accepté : seuls `.opencode` et `.test-build`
diffèrent (hors périmètre produit). Le code `app/`, `src/`, `tests/` est identique à
l'état accepté.

### 2. Correction présente dans la source acceptée
`src/components/theme.ts:19` : `textSecondary: '#3C6E8E'` (commentaire référençant
explicitement MOB-CYCLE32961708279-SEG et les mesures par fond). Même démarche que la
passe `textMuted` (jeton central ajusté, pas de couleur codée en dur localement).

### 3. Mesures WCAG 2.x indépendantes (calcul maison, seuil linéaire 0,04045)
`textSecondary #3C6E8E` sur chaque fond réel de l'inventaire :

| Fond | Valeur | Ratio | AA |
|------|--------|-------|----|
| background | #FEFEFE | 5,4607:1 | OK |
| surface | #FFFFFF | 5,5073:1 | OK |
| surfaceAlt | #F8F9FA | 5,2246:1 | OK |
| carte courante classement | #F7FCFB | 5,3164:1 | OK |
| carte Pro paywall | #FFFDF5 | 5,4078:1 | OK |
| secondary | #F1FAEE | 5,1530:1 | OK |
| NoticeBanner closeText | #EDF8F6 | 5,0769:1 | OK |
| TaskRow | #F6FCFA | 5,3036:1 | OK |

Ancien jeton `#457B9D` (référence du constat bloquant) : `surfaceAlt` 4,3564:1 et
`#F7FCFB` 4,4329:1 — les deux < 4,5:1, reproduisant exactement le constat hérité.

### 4. Inventaire complet et exhaustif
Énumération de tous les `backgroundColor` dans `app/` et `src/` : les seuls fonds
possibles sont les 8 ci-dessus, plus `rgba(38,70,83,0.35)` (scrim de modale, derrière la
carte de dialogue sur `background`), `#FFF4F1` (erreur de dialogue, qui utilise
`textPrimary` non `textSecondary`) et `transparent` (AppButton, sans `textSecondary`).
Chaque usage `textSecondary` identifié par grep se mappe vers un des 8 fonds couverts.
Aucune paire réelle orpheline.

### 5. Preuve de mutation (copie jetable, candidat jamais édité)
Dans `/tmp/opencode/mut` (copie de `.test-build`), `textSecondary` remplacé par
`#457B9D` : le test d'inventaire secondaire (subtest 4) échoue exactement, tandis que
les subtests 1-3 (textMuted, garde legacy textMuted, paires de remplacement) et le
subtest 5 (garde legacy `#457B9D`) restent verts. Aucune assertion existante retirée ni
affaiblie — la régression serait détectée.

### 6. Non-régression des acquis
- `textMuted #56707C` : toujours ≥ 4,5:1 sur ses 5 fonds (background 5,20, surface 5,24,
  surfaceAlt 4,98, #F7FCFB 5,06, #FFFDF5 5,15).
- Wrap du filtre membre (MOB-C4-F1) : `segmentedLayout.ts` intact, `SegmentedControl`
  toujours sur `surfaceAlt`.
- Frontières année / borne `now` (MOB-C4-F2) : couvertes par la suite passante.
- `npm run check` : typecheck OK, **164/164** tests passent.

### 7. Hors périmètre / honnêteté
Aucune dépendance, aucun réseau, aucun fichier hors `app/`, `src/`, `tests/` (delta zéro).
L'export Android (DRC-06) n'est pas ré-exécuté : le candidat est identique à l'état
accepté déjà attesté, et l'objectif borné porte sur le contraste, pas sur le build APK.

## Observations (non bloquantes)

- **MOB-RUN33111799778-OBS1 (info, mustFix:false)** : le commentaire d'inventaire du test
  (`tests/theme-contrast.test.ts:33`) liste « capNote du profil » parmi les usages
  `textSecondary` sur `surfaceAlt`, mais `app/(tabs)/profile.tsx:363-372` (style
  `capNote`) utilise en réalité `COLORS.textPrimary` (contraste supérieur). Inexactitude
  de documentation sans effet sur le contraste ni sur l'assertion. Correction facultative
  du commentaire uniquement.

## Décision

**accept**. La source acceptée satisfait le critère DRC-05 (MOB-CYCLE32961708279-SEG) :
chaque paire réelle `textSecondary`/fond atteint ≥ 4,5:1, l'inventaire secondaire est
couvert par un test déterministe avec preuve de mutation, et aucun acquis n'est régressé.
Aucun constat `mustFix: true`. Cet audit verification-only constitue une preuve auditable
courante que l'état accepté remplit l'objectif borné.
