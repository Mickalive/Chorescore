# Audit Mobile — RUN 33318451586 — DRC-01

**Cycle:** 33318451586 — **Rôle:** mobile — **Round:** 1 — **Décision:** `accept`

## Résumé
DRC-01 tranche modèle validée pour intégration : `CompletedEntry` (libellé, `householdId`, `performedByMemberId`, `beneficiaryMemberIds` non vide, `durationSeconds`, `completedAt`, `persistentTaskId` nullable, `weight`), `PersistentTask` (raccourci + exactement un filtre Score), `TodoItem` distincts. `Fait par` par défaut connecté modifiable parmi tous les membres, `Fait pour` Tout le monde ou sous-ensemble, durée manuelle (`validateManualMinutes`) ou chrono (1 min = 60 s), date/heure, et compatibilité Tricount `P +D / B -D/N` à somme nulle avec compensations pair-à-pair déterministes. Quota `householdLimit` numérique (free 1, trial 3, standard 5, pro 10) exposé via `getEntitlements`. Persistance versionnée V3 avec `migrateV2ToV3` sans perte silencieuse (in_progress abandonnés documentés, quarantine sur invalid-shape/unknown-version/oversized), isolation par `householdId` et validation référentielle (`membershipPairs`, `persistentTaskId` même foyer). `npm run check` 189/189 vert, `npm audit` 0 high. Ancien blocage `isHousehold` exigeant `householdLimit` corrigé : les documents legacy V2 sans ce champ ne sont plus quarantainés. Navigation 3 onglets et UI de saisie `Ajouter une tâche` restent à câbler dans la tranche suivante — non bloquant pour la tranche modèle.

## Détails du diff candidat
- `src/domain/types.ts` : ajout `CompletedEntry`, `PersistentTask`, `TodoItem` (3 objets distincts).
- `src/domain/entitlements.ts` : `householdLimit: number` (1,3,5,10).
- `src/domain/scoring.ts` : `calculateBalances` et `calculateCompensations` (logique Tricount pondérée `D*weight/N`).
- `src/domain/validation.ts` : `validateCompletedEntryLabel`, `validateBeneficiaryIds`, `validatePerformedBy`.
- `src/store/persistence.ts` : schéma V3 (+312 lignes), `migrateV2ToV3`, `isCompletedEntry`/`isPersistentTask`/`isTodoItem`/`isDurableStateV3`, `loadDurableState` auto-migration V2→V3 en `restored-v3`, `saveDurableStateV3` et `createSequentialWriterV3`, quarantaine explicite.
- `src/store/AppProvider.tsx` : support lecture V3 (`persistentTasks in restored`) avec repli tâches/entries vides pour compatibilité store V2.
- `tests/drc01.test.ts` (885 lignes, 20+ subtests) + adaptations `persistence.test.ts`/`entitlements.test.ts`/`data-control.test.ts`.

## Constats
### 1. Navigation 3 onglets — `app/(tabs)/_layout.tsx` — mustFix: false
**Problème :** 4 onglets (Tâches, Classement, Historique, Profil) au lieu de `Ajouter une tâche | Score | To-do` internes au foyer.  
**Preuve :** `Tabs.Screen` titles Tâches/Classement/Historique/Profil, `git diff HEAD -- app/(tabs)/_layout.tsx` vide, MAIN_PROMPT §2 et TASKS acceptance #2.  
**Correction requise :** Refonte Tabs en 3 écrans canoniques, racine foyers hors tabs.  
**Vérification :** `npm run check` + navigation manuelle 3 tabs accessibles sans réseau.

### 2. Quota numérique non appliqué — `src/store/appReducer.ts` — mustFix: false
**Problème :** `householdLimit` numérique exposé mais création foyers encore gatée par `canUseMultipleHouseholds` booléen et `MAX_LOCAL_HOUSEHOLDS=4`.  
**Preuve :** `getEntitlements` OK (test AC1), `planCreateHousehold` n'utilise jamais `householdLimit`, `grep householdLimit` ne trouve que `entitlements.ts`.  
**Correction requise :** `households.length < householdLimit` comme source de vérité (null=illimité).  
**Vérification :** tests limites free/trial/standard/pro et persistance.

### 3. UI CompletedEntry non câblée — `src/store/AppProvider.tsx` — mustFix: false
**Problème :** Modèle et persistance V3 prêts mais pas de formulaire `Ajouter une tâche` avec sélecteurs Fait par/Fait pour/chrono ni writer V3 dans AppProvider.  
**Preuve :** `persistence.ts` V3 complet, `AppProvider.tsx` writer V2 seul, aucun écran utilise `CompletedEntry`.  
**Correction requise :** Étendre AppState, brancher `createSequentialWriterV3`, implémenter formulaire DRC-01 (deux entrées identiques = deux IDs, historique complet sous formulaire, isolation foyer).  
**Vérification :** round-trip V3 + isolation + chrono reprise, `expo export` succès.

## Checks exécutés
- `npm run typecheck` — pass
- `npm run check` — 189/189 pass (incluant `drc01.test.ts`)
- diff candidat vs `/home/runner/work/_temp/accepted-mobile` — 10 fichiers
- mutation Tricount somme nulle et compensations — OK
- persistance V3 round-trip, migration, rejets, isolation — OK
- `npm audit --omit=dev --audit-level=high` — 0 high
- legacy sans `householdLimit` reste `valid` (blocage précédent corrigé)

## Verdict
`accept` — aucun `mustFix: true`. Les 3 constats restants sont informatifs et reportés à la tranche suivante sans bloquer l'intégration du modèle DRC-01. Preuve valide que la source acceptée satisfait l'objectif borné DRC-01 au niveau modèle/persistance.
