# Audit Mobile — RUN 33336517880 — DRC-01 Repair Fait par

**Cycle:** 33336517880 — **Rôle:** mobile — **Round:** 1 — **Décision:** `accept`
**Critère assigné:** DRC-01 — Repair rendre Fait par modifiable (blocage cycle 33328400903)

## Verdict
`accept` — le blocage unique est levé. Le champ **Fait par** est désormais un sélecteur modifiable listant tous les membres du foyer, défaut = `currentUserId`, valeur stockée dans `performedByMemberId` et utilisée dans `handleSubmit` via `planManualEntry`/`AppProvider`. Aucun `mustFix:true` ne subsiste pour cette tranche ; DRC-01 peut passer `complete`. Les écrans Score et To-do restent placeholders comme prévu pour DRC-03/DRC-04 (non bloquant).

## Preuves exécutées
- `npm run check` — typecheck 0 erreur, **195/195** tests verts (dont 6 nouveaux `DRC-01-AC16` sur `planManualEntry` : currentUser ok, autre membre ok, intrus rejette, vide rejette, outsider autre foyer rejette, IDs distincts). 189 tests historiques non régressés.
- `EXPO_PUBLIC_DATA_MODE=demo npx expo export --platform android` — success, 1288 modules, 2.9MB hbc, `dist/metadata.json` présent.
- Diff réel candidat vs `accepted-mobile` (HEAD 8008532) — 8 fichiers : `app/(tabs)/index.tsx` (householdMembers + prop drilling), `src/components/ManualEntryModal.tsx` (SegmentedControl + `validatePerformedBy`), `src/store/AppProvider.tsx` (`addManualEntry(taskId, minutes, performedByMemberId)`), `src/store/appReducer.ts` (`planManualEntry` 4e param + validation membership).
- Câblage vérifié : `householdMembers = users.filter(membership householdId)`, `currentUserId` passé en prop, `SegmentedControl` avec `accessibilityLabel="Sélectionner le membre qui a réalisé la tâche"` et `wrap`, `value/onChange` sur `performedByMemberId`, `submit` appelle `onSubmit(parsed, performedByMemberId)` et non un performer fixe.
- Mutation proof : `validatePerformedBy('')` → erreur, `planManualEntry('', …)` rejette, membre inconnu `user_intrus` rejette, membre d'un autre foyer rejette, membre valide accepte et est persisté via `appDataService.createManualEntry({userId: performedByMemberId})`.

## Constats (tous mustFix:false → accept)
### 1) `src/components/ManualEntryModal.tsx` — Fait par sélecteur conforme
- **Problème :** Aucun — le sélecteur est implémenté et conforme à MAIN_PROMPT §3 et aux acceptances #1-#5.
- **Preuve :** props `householdMembers:User[], currentUserId:string, onSubmit(minutes, performedByMemberId)`, `useState(currentUserId)`, reset `useEffect` sur `task null` → `currentUserId`, `validatePerformedBy` puis `onSubmit(parsed, performedByMemberId)`, `householdMembers.length>1 ? <SegmentedControl options=householdMembers.map … value performedByMemberId onChange setPerformedByMemberId wrap /> : null`.
- **Correction :** Aucune.
- **Vérification :** Ouvrir Ajouter une tâche en démo (3 membres) : Fait par propose 3 segments, défaut Noa. Changer vers Camille → 30 min → entrée créée avec `performedByMemberId=user_camille` (tests AC16 6/6).

### 2) `src/store/appReducer.ts` + `AppProvider.tsx` — plan métier et persistance
- **Problème :** Aucun — validation membership et stockage corrects.
- **Preuve :** `planManualEntry(state, taskId, minutes, performedByMemberId)` vérifie `validateManualMinutes`, `performedByMemberId.length===0`, `isMember = memberships.some(m.householdId===state.household.id && m.userId===performedByMemberId)` sinon erreur foyer, retourne `performedByMemberId`. `AppProvider.addManualEntry` utilise `plan.value.performedByMemberId` pour `userId`.
- **Correction :** Aucune.
- **Vérification :** Tests AC16 couvrent vide/inconnu/hors-foyer/autre membre/distinct IDs — tous verts.

### 3) `app/(tabs)/index.tsx` — masquage mono-membre (cosmétique)
- **Problème :** Quand un foyer n'a qu'un membre, le sélecteur est masqué (`length>1 ? … : null`). Divergence mineure mais sans choix réel, la valeur par défaut reste correcte.
- **Preuve :** `L123-137 {householdMembers.length > 1 ? <SegmentedControl …/> : null}` — demo a 3 membres donc visible en pratique.
- **Correction :** Optionnel hors DRC-01 : afficher même à 1 membre ou conserver.
- **Vérification :** Foyer mono-membre → pas de Fait par visible mais soumission réussit avec `currentUserId`. Non bloquant.

### 4) `app/(tabs)/score.tsx` + `todo.tsx` — placeholders attendus
- **Problème :** Score et To-do restent placeholders, conforme à la tranche DRC-01 ; travail DRC-03/DRC-04 à venir.
- **Preuve :** Écrans accessibles sans balances/compensations ni Todo→CompletedEntry, déjà mustFix:false en 33328400903.
- **Correction :** Aucune pour DRC-01.
- **Vérification :** Navigation démo : 3 onglets accessibles sans réseau.

## Conformité DRC-01 acceptances
- [x] Fait par sélecteur listant tous les membres (chips/SegmentedControl/Picker) — `SegmentedControl` avec tous les `householdMembers`.
- [x] Défaut = `currentUserId` — `useState(currentUserId)` + reset.
- [x] Modifiable avant soumission et stocké dans `performedByMemberId` — state + `onChange`.
- [x] `handleSubmit` utilise la valeur sélectionnée — `onSubmit(parsed, performedByMemberId)` → `planManualEntry` → `createManualEntry`.
- [x] Survit à la création et se réinitialise après succès — `useEffect` sur `task null`.
- [x] Tests UI `validatePerformedBy` + choix autre membre — 6 tests AC16 verts.
- [x] `npm run check` vert et export Android succès.
- [x] 195 tests verts, 0 régression.
- [x] 3 onglets fonctionnels en demo sans réseau.

## Recommandation d'intégration
Intégrable en `lab/chorescore`. DRC-01 `PRODUCT-RESET-CORE` peut être marqué `resolved`.
