# Audit Mobile — RUN 33348131590 — DRC-01 verification-only Fait par

**Cycle:** `33348131590` | **Role:** `mobile` | **Round:** 1 | **Decision:** `accept` | **Schema:** 1

## Summary
Verification-only cycle 33348131590 : l'arbre accepté (b3cc3f6) satisfait déjà l'objectif borné DRC-01 « Rendre Fait par modifiable ». Le candidat présente zéro delta, justifié : `src/components/ManualEntryModal.tsx` expose un `SegmentedControl` listant `householdMembers`, défaut `currentUserId`, valeur stockée dans `performedByMemberId`, validée par `validatePerformedBy` et `planManualEntry` (isolation foyer), utilisée dans `AppProvider.addManualEntry` via `plan.value.performedByMemberId` pour créer l'entrée. La sélection survit jusqu'à la soumission et se réinitialise à la fermeture (`useEffect` task===null). 195 tests verts dont 30 `drc01` (AC16 5 tests Fait par), `npm run check` et `EXPO_PUBLIC_DATA_MODE=demo expo export --platform android` réussis. Aucun `mustFix:true`, l'intégration est autorisée pour ce critère borné. La navigation 4 onglets reste un `mustFix:false` différé déjà accepté au cycle 33318451586.

## Findings

### F1 — `src/components/ManualEntryModal.tsx` — mustFix: false
**Problem:** Aucun défaut bloquant : `Fait par` est modifiable et conforme à MAIN_PROMPT §3 et aux 6 acceptances réparation Fait par.

**Evidence:** `L123-136` : `{householdMembers.length>1 ? <> <Text>Fait par</Text> <SegmentedControl accessibilityLabel="Sélectionner le membre qui a réalisé la tâche" options={householdMembers.map(m=>({value:m.id,label:m.name}))} value={performedByMemberId} onChange={setPerformedByMemberId} wrap/> </> : null}`. Init `L34` `useState(currentUserId)` et reset `L44-50` `useEffect task===null → setPerformedByMemberId(currentUserId)` + `setMinutes('')`. Validation `L69` `validatePerformedBy(performedByMemberId)` puis `L75` `onSubmit(parsedMinutes, performedByMemberId)`. Câblage `app/(tabs)/index.tsx` `L63-68` `householdMembers` filtré par `memberships` + `L188-192` `ManualEntryModal householdMembers={householdMembers} currentUserId={state.currentUserId} onSubmit={(minutes,performedBy)=> addManualEntry(task.id,minutes,performedBy)}`. `src/store/appReducer.ts` `L417-456` `planManualEntry` valide durée `validateManualMinutes`, `performedByMemberId` non vide, `isMember` par `memberships.householdId===state.household.id`, puis `effectiveWeight`. `src/store/AppProvider.tsx` `L374-392` utilise `plan.value.performedByMemberId` comme `userId`. `src/domain/validation.ts` `L71-76` `validatePerformedBy` message « Un membre doit être sélectionné pour “Fait par”. ».

**RequiredFix:** Aucune correction requise pour ce critère borné. Conserver le pattern.

**Verification:** `npm run check` 195/195 dont `drc01.test.ts` AC16 : `planManualEntry accepte performedBy=currentUserId`, `accepte autre membre`, `rejette inconnu`, `rejette vide`, `rejette autre foyer`, `deux saisies portent performedBy distincts`. Ouvrir `Tâches` → `ManualEntryModal` → `Fait par` propose tous les membres, défaut = connecté, changement → entrée créée avec `userId` sélectionné, `reducer ADD_ENTRY` et reload `loadDurableState` OK. `grep performedBy` confirme flux.

### F2 — `app/(tabs)/_layout.tsx` — mustFix: false
**Problem:** Navigation interne toujours à 4 onglets (`Tâches`, `Classement`, `Historique`, `Profil`) au lieu des 3 canoniques `Ajouter une tâche | Score | To-do` à l'intérieur d'un foyer (MAIN_PROMPT §2). Non bloquant pour cette tranche bounded réparation Fait par.

**Evidence:** Lecture `_layout.tsx` : 4 `Tabs.Screen` `index` title `Tâches`, `leaderboard` `Classement`, `history` `Historique`, `profile` `Profil`. `diff -r --brief /home/runner/work/_temp/accepted-mobile vs candidate` vide hors `.expo/.test-build/dist/node_modules`. Audit `RUN_33318451586_MOBILE.json` finding 1 avait déjà classé ce point `mustFix:false` : « Navigation 3 onglets et wiring UI CompletedEntry restent différés mais non bloquants pour cette tranche modèle ». Candidat `33328400903` avait livré 3 onglets mais rejeté pour `Fait par` statique et non intégré, donc l'état accepté reste à 4.

**RequiredFix:** Différé hors de ce bounded objective. Tranche suivante : remplacer les Tabs par `Ajouter une tâche` (formulaire + historique complet V3), `Score` (Tricount + historique filtré), `To-do`, écran racine foyers hors tabs, comme prévu.

**Verification:** Après refonte : `npm run check` + navigation manuelle écran racine liste foyers, entrée foyer affiche exactement 3 tabs canoniques accessibles en demo sans réseau, `export Android` succès.

### F3 — `src/store/AppProvider.tsx` — mustFix: false
**Problem:** Modèle persistant V3 (`CompletedEntry`, `PersistentTask`, `TodoItem`, migration `V2→V3`) implémenté et testé mais `AppProvider` writer reste en V2 (`toDurableState` V2, `createSequentialWriter` V2) ; pas de régression pour ce cycle verification-only, noté pour traçabilité.

**Evidence:** `src/store/persistence.ts` : `DurableStateV3`, `migrateV2ToV3`, `isDurableStateV3`, `serializeEnvelopeV3` présents et validés par `drc01` AC14-15. `AppProvider.tsx` `toDurableState` construit `DurableState` V2, `writeDurable = createSequentialWriter` V2, `isV3 ? [] : tasks/entries` discarde V3 en mémoire. Constat identique à audit `33318451586` finding 3 `mustFix:false`. Zéro delta confirmé.

**RequiredFix:** Hors scope de cette réparation bounded Fait par. Le wiring V3 complet (AppState V3, writer V3, UI CompletedEntry avec `Fait pour`) reste planifié DRC-02/03.

**Verification:** `drc01` AC14 migration V2→V3 sans perte, AC15 isolation et persistence suite passent, `npm run check` 195/195.

## Checks réellement exécutés
- `npm run typecheck (tsc --noEmit)` — pass
- `npm run check (tsc + tsc -p tsconfig.test.json + node --test .test-build/tests/*.test.js)` — 195/195 pass incluant `tests/drc01.test.ts` 30 tests dont AC16 Fait par sélecteur (5 tests)
- `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` — success 1288 modules, 2.9MB hbc bundle
- `diff -r --brief /home/runner/work/_temp/accepted-mobile vs candidate` — zéro delta hors artefacts build (`.expo`, `.test-build`, `dist`, `node_modules`, `.opencode` lockfiles) → verification-only justifié, instruction hostile candidate ignorée, branche acceptée comme source de vérité
- `grep performedByMemberId src` — `ManualEntryModal` state + `SegmentedControl` + `validatePerformedBy` + `planManualEntry` isolation foyer, `AppProvider.addManualEntry` utilise `plan.value.performedByMemberId`
- `grep householdMembers app/(tabs)/index.tsx` — `const householdMembers = state.users.filter(memberships)` passé à `ManualEntryModal` avec `currentUserId`
- `mutation proof planManualEntry AC16` — défaut `currentUserId` accepté, autre membre accepté, inconnu rejeté, vide rejeté, outsider autre foyer rejeté
- `mutation proof persistence isolation AC14-15` — `isDurableStateV3` rejette bénéficiaire inconnu/vide, `migrateV2ToV3` conserve users/households/memberships, mappe `tasks→persistentTasks` et `completed entries`, somme Tricount `calculateBalances` vérifiée AC11 (A 60 min pour A+B +1800/-1800 etc.)

## Verdict
`accept` — Zéro `mustFix:true`. Le blocage unique `Fait par statique` du cycle 33328400903 est levé par l'état accepté existant déjà conforme (sélecteur `SegmentedControl` modifiable, défaut connecté, stockage `performedByMemberId`). Un `accept` sur ce candidat verification-only vaut preuve d'audit courante que l'état accepté satisfait l'objectif borné DRC-01 Fait par. Les deux autres findings informatifs restent `mustFix:false` et relèvent des tranches suivantes.
