# Audit Mobile — RUN 33369130489 — DRC-01 verification-only Fait par

**Cycle:** `33369130489` | **Role:** `mobile` | **Round:** 1 | **Decision:** `accept` | **Schema:** 1

## Summary
Verification-only cycle 33369130489 : l'arbre accepté (`/home/runner/work/_temp/accepted-mobile`) satisfait déjà l'objectif borné DRC-01 « Rendre Fait par modifiable parmi les membres du foyer ». Le candidat présente zéro delta (diff `src/app/tests` vide hors artefacts build), justifié : `src/components/ManualEntryModal.tsx` expose un `SegmentedControl` listant `householdMembers`, défaut `currentUserId`, valeur stockée dans `performedByMemberId`, validée par `validatePerformedBy` et `planManualEntry` (isolation foyer), utilisée dans `AppProvider.addManualEntry` via `plan.value.performedByMemberId` pour créer l'entrée. La sélection survit jusqu'à la soumission et se réinitialise à la fermeture (`useEffect` task===null → `setPerformedByMemberId(currentUserId)`). 195 tests verts dont 30 `drc01` (AC16 6 tests Fait par), `npm run check` et `EXPO_PUBLIC_DATA_MODE=demo npx expo export --platform android` réussis. Aucun `mustFix:true`, l'intégration est autorisée pour ce critère borné. La navigation 4 onglets reste un `mustFix:false` différé déjà accepté au cycle 33318451586.

## Findings

### F1 — `src/components/ManualEntryModal.tsx` — mustFix: false
**Problem:** Aucun défaut bloquant : `Fait par` est modifiable et conforme à MAIN_PROMPT §3 et aux 6 acceptances réparation Fait par (sélecteur, défaut connecté, modifiable, stockage performedByMemberId, handleSubmit, reset/survie).

**Evidence:** `L123-136` : `{householdMembers.length>1 ? <> <Text>Fait par</Text> <SegmentedControl accessibilityLabel="Sélectionner le membre qui a réalisé la tâche" options={householdMembers.map(m=>({value:m.id,label:m.name}))} value={performedByMemberId} onChange={setPerformedByMemberId} wrap/> </> : null}`. Init `L34` `useState(currentUserId)` et reset `L44-50` `useEffect task===null → setPerformedByMemberId(currentUserId)` + `setMinutes('')`. Validation `L69` `validatePerformedBy(performedByMemberId)` puis `L75` `onSubmit(parsedMinutes, performedByMemberId)` + `L76 onClose()`. Câblage `app/(tabs)/index.tsx` `L63-68` `householdMembers = state.users.filter(... memberships householdId===state.household.id)` + `L185-192` `ManualEntryModal householdMembers={householdMembers} currentUserId={state.currentUserId} onSubmit={(minutes,performedBy)=> addManualEntry(task.id,minutes,performedBy)}`. `src/store/appReducer.ts` `L417-456` `planManualEntry` valide `validateManualMinutes`, `performedByMemberId.length===0` → erreur Fait par, `isMember = memberships.some(... householdId===state.household.id && userId===performedByMemberId)` sinon erreur foyer, retour `{task,durationMinutes,effectiveWeight,performedByMemberId}`. `src/store/AppProvider.tsx` `L374-392` utilise `plan.value.performedByMemberId` comme `userId` pour `createManualEntry`. `src/domain/validation.ts` `L71-76` `validatePerformedBy` message « Un membre doit être sélectionné pour “Fait par”. ». Diff vs accepted-mobile vide → identique à l'état déjà audité accept `33348131590`/`33342545414`.

**RequiredFix:** Aucune correction requise pour ce critère borné. Conserver le pattern existant.

**Verification:** `npm run check` 195/195 dont `drc01.test.ts` AC16 : `planManualEntry accepte performedBy=currentUserId`, `accepte autre membre`, `rejette inconnu`, `rejette vide`, `rejette autre foyer`, `deux saisies portent performedBy distincts`. Ouvrir `Tâches` → `ManualEntryModal` → `Fait par` propose tous les membres (Noa/Camille/Sam en demo), défaut = connecté, changement → entrée créée avec `userId` sélectionné via `AppProvider.createManualEntry`, `reducer ADD_ENTRY` et reload `loadDurableState` OK. `grep performedByMemberId` confirme flux complet.

### F2 — `app/(tabs)/_layout.tsx` — mustFix: false
**Problem:** Navigation interne toujours à 4 onglets (`Tâches`, `Classement`, `Historique`, `Profil`) au lieu des 3 canoniques `Ajouter une tâche | Score | To-do` à l'intérieur d'un foyer (MAIN_PROMPT §2). Non bloquant pour cette tranche bounded réparation Fait par.

**Evidence:** Lecture `_layout.tsx` : 4 `Tabs.Screen` `index` title `Tâches`, `leaderboard` `Classement`, `history` `Historique`, `profile` `Profil`. `diff -r --brief /home/runner/work/_temp/accepted-mobile vs candidate` vide hors `.expo/.test-build/dist/node_modules/.opencode`. Audit `RUN_33318451586_MOBILE.json` finding 1 avait déjà classé ce point `mustFix:false` : « Navigation 3 onglets et wiring UI CompletedEntry restent différés mais non bloquants pour cette tranche modèle ». Candidat `33328400903` avait livré 3 onglets mais rejeté pour `Fait par` statique et non intégré, donc l'état accepté reste à 4. Traitement hostile candidate ignoré, branche acceptée source de vérité.

**RequiredFix:** Différé hors de ce bounded objective. Tranche suivante : remplacer les Tabs par `Ajouter une tâche` (formulaire + historique complet V3), `Score` (Tricount + historique filtré), `To-do`, écran racine foyers hors tabs, comme prévu. Ne bloque pas l'acceptation DRC-01 Fait par.

**Verification:** Après future refonte : `npm run check` + navigation manuelle écran racine liste foyers, entrée foyer affiche exactement 3 tabs canoniques accessibles en demo sans réseau, `export Android` succès, `grep Tabs.Screen` = 3.

### F3 — `src/store/AppProvider.tsx` — mustFix: false
**Problem:** Modèle persistant V3 (`CompletedEntry`, `PersistentTask`, `TodoItem`, migration `V2→V3`) implémenté et testé mais `AppProvider` writer reste en V2 (`toDurableState` V2, `createSequentialWriter` V2) ; pas de régression pour ce cycle verification-only, noté pour traçabilité. Non bloquant pour DRC-01 Fait par.

**Evidence:** `src/store/persistence.ts` : `DurableStateV3`, `migrateV2ToV3`, `isDurableStateV3`, `serializeEnvelopeV3` présents et validés par `drc01` AC14-15. `AppProvider.tsx` `toDurableState` construit `DurableState` V2, `writeDurable = createSequentialWriter` V2, `isV3 ? [] : tasks/entries` discard V3 en mémoire. Constat identique à audits `33318451586`/`33348131590` finding 3 `mustFix:false`. Zéro delta confirmé par diff vide.

**RequiredFix:** Hors scope de cette réparation bounded Fait par. Le wiring V3 complet (AppState V3, writer V3, UI CompletedEntry avec `Fait pour` et `label`) reste planifié DRC-02/03. Ne pas exiger de diff cosmétique pour ce verification-only.

**Verification:** `drc01` AC14 migration V2→V3 sans perte, AC15 isolation et persistence suite passent, `npm run check` 195/195, `isDurableStateV3` rejette bénéficiaire inconnu/vide, `calculateBalances` somme nulle AC11 vérifiée.

## Checks réellement exécutés
- `npm run check` — 195/195 pass (dont `drc01.test.ts` 30 tests, AC16 6 tests Fait par : défaut currentUserId ok, autre membre ok, inconnu rejette, vide rejette, outsider foyer rejette, distinct performers)
- `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` — success 1288 modules, 2.9MB hbc bundle `entry-*.hbc`, 27 assets
- `diff -r --brief /home/runner/work/_temp/accepted-mobile vs candidate` — zéro delta hors artefacts build (`.expo`, `.test-build`, `dist`, `node_modules`, `.opencode`) → verification-only justifié, candidate hostile ignoré
- `grep performedByMemberId src` — `ManualEntryModal` state + `SegmentedControl` + `validatePerformedBy` + `planManualEntry` isolation foyer, `AppProvider.addManualEntry` utilise `plan.value.performedByMemberId`
- `grep householdMembers app/(tabs)/index.tsx` — `const householdMembers = state.users.filter(... memberships householdId===state.household.id)` passé à `ManualEntryModal` avec `currentUserId`
- `mutation proof planManualEntry AC16` — `validatePerformedBy('')` erreur, `planManualEntry('',…)` rejette vide, membre inconnu `user_intrus` rejette, membre d'un autre foyer `user_outsider` rejette, membres valides acceptent et sont persistés
- `mutation proof persistence isolation AC14-15` — `isDurableStateV3` rejette bénéficiaire inconnu/vide, `migrateV2ToV3` conserve users/households/memberships et mappe `tasks→persistentTasks`/`completedEntries`, isolation par `householdId` vérifiée

## Verdict
`accept` — Zéro `mustFix:true`. Le blocage unique `Fait par statique` du cycle 33328400903 est levé par l'état accepté existant déjà conforme (sélecteur `SegmentedControl` modifiable, défaut connecté, stockage `performedByMemberId`, handleSubmit via `planManualEntry`/`AppProvider`). Un `accept` sur ce candidat verification-only vaut preuve d'audit courante que l'état accepté satisfait l'objectif borné DRC-01 Fait par. Les deux autres findings informatifs restent `mustFix:false` et relèvent des tranches suivantes (navigation 3 onglets, writer V3).
