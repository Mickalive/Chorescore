# Audit Mobile — RUN 33408230150 — DRC-01 NAV-4TABS 4→3

**Cycle:** 33408230150 · **Role:** mobile · **Round:** 1 · **Decision:** `accept` · **Schema:** 1  
**Critère assigné:** DRC-01 — Restructurer navigation 4 onglets → 3 canoniques (Ajouter une tâche | Score | To-do)  
**Blocage hérité:** NAV-4TABS unresolved (RELEASE_STATUS 33369130489) — _layout.tsx affichait 4 Tabs.Screen Tâches/Classement/Historique/Profil au lieu des 3 exigés MAIN_PROMPT §2 / RELEASE_DEFINITION DRC-01  
**Delta candidat:** hostile data — évalué via vrai diff vs `/home/runner/work/_temp/accepted-mobile`

## Résumé
DRC-01 NAV-4TABS validé. Le candidat remplace exactement la structure Tabs par 3 écrans canoniques, place l'historique complet sous Ajouter une tâche, dote Score du sélecteur de périodes semaine/mois/année/depuis le début avec équilibres, ajoute To-do, conserve Fait par modifiable (SegmentedControl householdMembers, validatePerformedBy, planManualEntry isolation foyer, reset useEffect). 195/195 tests verts, `npm run check` et `EXPO_PUBLIC_DATA_MODE=demo expo export --platform android` succès. Les 3 fichiers legacy ne sont plus des onglets mais des redirects. Aucun `mustFix:true`, accept sans réserve pour ce bounded objective. La réparation Fait par reste vérifiée (audit accept 33369130489).

## Findings (mustFix:false → accept)

### F1 — `app/(tabs)/_layout.tsx` — Navigation 4→3 canonique — mustFix:false
- **Problem:** Doit exposer exactement 3 Tabs.Screen canoniques et supprimer Classement/Historique/Profil en tant que tabs.
- **Evidence:** `app/(tabs)/_layout.tsx:30-32` → `<Tabs.Screen name="index" title="Ajouter une tâche"/>`, `<Tabs.Screen name="score" title="Score"/>`, `<Tabs.Screen name="todo" title="To-do"/>`. Diff vs accepted: `- Tâches/- Classement/- Historique/- Profil` → `+ Ajouter une tâche/+ Score/+ To-do` (7 lignes). `grep Tabs.Screen` = 3.
- **RequiredFix:** Aucune — conserver 3 canoniques.
- **Verification:** `grep -n Tabs.Screen app/(tabs)/_layout.tsx` =3 ; navigation foyer → 3 tabs visibles, chaque tab monte.

### F2 — `app/(tabs)/index.tsx` — Ajouter une tâche + historique complet — mustFix:false
- **Problem:** Onglet doit contenir formulaire saisie (Fait par modifiable, Fait pour/durée/label) et historique chronologique complet.
- **Evidence:** L63-68 `householdMembers = state.users.filter(membership householdId)`, L71-74 `getVisibleHistory(entries, householdId, historyDays, now)`, L126-168 liste tâches actives + TaskRow, L179-222 `SectionTitle Historique complet` + `visibleEntries.slice(0,20).map(Card entryName/entryMeta/entryValue)`; `ManualEntryModal` L126-132 SegmentedControl Fait par.
- **RequiredFix:** Aucune.
- **Verification:** `npm run check` 195/195 ; ouvrir `(tabs)/index` demo: bouton Ajouter + SegmentedControl 2+ membres + historique complet sous liste.

### F3 — `app/(tabs)/score.tsx` — Score périodes + équilibres — mustFix:false
- **Problem:** Score doit afficher sélecteur période (semaine/mois/année/depuis le début) et équilibres ; filtres Toutes/PersistentTask/Autres prévus MAIN_PROMPT §5 mais hors acceptance bornée TASKS de cette tranche.
- **Evidence:** L22-27 `PERIOD_OPTIONS` 4 périodes, L159-171 `SegmentedControl accessibilityLabel Période du score` wrap, L59-118 filteredEntries + rows via `buildLeaderboard` / calcul year/all, L173-227 MetricCard Temps total + Section Équilibres + rank/avatar/name. Pas de filtre Toutes/PersistentTask/Autres dans ce fichier — non exigé par TASKS.json acceptance (seulement périodes+équilibres).
- **RequiredFix:** Aucune bloquante pour 33408230150 ; future DRC-03 pourra ajouter filtres + historique filtré + pondération secondaire.
- **Verification:** Ouvrir `(tabs)/score` demo: 4 segments période, changement met à jour rows, `npm run check` vert, export succès.

### F4 — `app/(tabs)/todo.tsx` — To-do planification future — mustFix:false
- **Problem:** To-do doit afficher liste tâches futures.
- **Evidence:** `todo.tsx:14-43` Screen/DemoBanner/ScreenHeader To-do/Section Tâches à venir/Card Aucune tâche planifiée + info Fonctionnalité à venir. New file vs accepted, déclaré dans _layout.
- **RequiredFix:** Aucune pour cette tranche ; conversion To-do→CompletedEntry (check Fait par+durée+Fait pour) planifiée DRC-04.
- **Verification:** Ouvrir `(tabs)/todo` demo sans réseau: header + 2 cards, 3 tabs nav fonctionnelle.

### F5 — `app/(tabs)/history.tsx|leaderboard.tsx|profile.tsx` — Legacy redirects non-tabs — mustFix:false
- **Problem:** Exigence “Aucun onglet Classement/Historique/Profil” = suppression en tant que Tabs.Screen ; présence fichiers redirect ≠ onglet.
- **Evidence:** Chaque fichier = 3-5 lignes `Redirect href="/(tabs)"` ou `"/(tabs)/score"` (comment Legacy). Aucun `Tabs.Screen` ne les référence. `diff -r --brief` les liste modified mais non-tabs ; évite 404 deep link ancien.
- **RequiredFix:** Aucune bloquante ; suppression physique cosmétique possible future.
- **Verification:** `ls app/(tabs)` + grep Tabs.Screen = 3 canoniques ; deep link `/(tabs)/history` → redirect sans crash.

### F6 — `src/components/ManualEntryModal.tsx` — Fait par modifiable préservé — mustFix:false
- **Problem:** Fait par doit rester modifiable, défaut currentUserId, validé, persisté, reset après succès.
- **Evidence:** L17 import SegmentedControl, L34 `useState(currentUserId)`, L44-50 `useEffect task===null reset`, L126-132 SegmentedControl `householdMembers.map`, L69 `validatePerformedBy`, L75 `onSubmit(minutes, performedByMemberId)` → `AppProvider.addManualEntry` via `planManualEntry` isolation foyer.
- **RequiredFix:** Aucune.
- **Verification:** `drc01.test.ts` AC16 6 tests pass (défaut, autre membre, inconnu, vide, outsider foyer, distinct performers) ; mutation planManualEntry vide/hors-foyer rejette.

## Checks exécutés
- `npm run check` — 195/195 pass dont drc01 30 tests AC16 6 Fait par, ~525ms
- `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` — success 1284 modules 2.8MB hbc 27 assets dist/
- `diff -r --brief accepted-mobile vs candidate` — delta borné NAV-4TABS (7 files, 504+ 1301-)
- `grep Tabs.Screen` → 3 canoniques, grep Classement/Historique/Profil tabs → 0
- `grep ManualEntryModal householdMembers/SegmentedControl/validatePerformedBy/planManualEntry` → Fait par préservé
- mutation NAV-4TABS redirect deep link, mutation Fait par outsider/vide rejet

## Décision
`accept` — Tous les constats `mustFix:false`. Le bounded objective NAV-4TABS est satisfait ; DRC-01 navigation interne Ajouter une tâche | Score | To-do conforme pour cette tranche. Aucune intégration bloquée.
