# Audit Mobile — RUN 33472877686 — DRC-03 filtres Score + historique contextuel + correction journal

**Cycle:** `33472877686` | **Role:** `mobile` | **Round:** 1 | **Decision:** `accept` | **Schema:** 1

## Summary
Cycle 33472877686 DRC-03 : candidate à delta produit ciblé (5 fichiers, +784/-10) implémente intégralement le reste DRC-03 — filtres Score `Toutes | PersistentTask | Autres`, historique contextuel filtré sous les stats, graphes à barres avec noms lisibles sans identité couleur, vue pondérée secondaire conditionnelle, et correction/suppression entrées libres du journal (PRODUCT-RESET-DATA). `scoreFilters.ts` fournit `buildScoreFilterOptions/filterEntriesByTask/hasArchivedTaskEntries/buildMemberBarData/hasWeightedContent` avec 16 tests dédiés (`tests/drc03-score-filters.test.ts`), chaînage `période → filtre tâche` correct, `Autres` regroupant exclusivement les entrées dont `taskId` n'est plus une tâche active du foyer (une PersistentTask = un filtre, pas de filtre depuis libellé ponctuel). `MemberBarChart.tsx` rend le nom du membre à côté de chaque barre (80px `numberOfLines 1`), minutes arrondies et valeur pondérée, accessibilité `name, X minutes, N entrées`. `score.tsx` ajoute `SegmentedControl Filtre de tâche` + `MetricCard` sur `filteredEntries` + `Historique filtré` (30 entrées max, `tâche archivée` fallback) + vue pondérée si `useWeights && weightSnapshot≠1`. `index.tsx` expose `✏️/🗑️` sur ses propres entrées terminées via `EntryCorrectionModal` (poids figé, preview LiveRegion) et `deleteEntry` confirmée, avec isolation foyer validée par `planEditEntryDuration/planDeleteEntry`. `npm run check` 211/211 verts (195 existants +16 DRC-03), `EXPO_PUBLIC_DATA_MODE=demo expo export --platform android` succès (1287 modules, 2.8MB hbc), `functions` inchangé. Aucun `mustFix:true` → `accept`, DRC-03 peut passer `complete`.

## Findings

### F1 — `app/(tabs)/score.tsx` — mustFix: false
**Problem:** Score doit afficher un sélecteur de filtres : `Toutes | une entrée par PersistentTask | Autres` (MAIN_PROMPT §5 `Une PersistentTask = un filtre. Libellés libres → Autres`, TASKS acceptance 1).

**Evidence:** `score.tsx` L246-253 `SegmentedControl accessibilityLabel="Filtre de tâche" options={taskFilterOptions} value={taskFilter} onChange={setTaskFilter} wrap`. `taskFilterOptions` L86-89 `buildScoreFilterOptions(state.tasks, household.id, archivedExist)`. `scoreFilters.ts` L32-51 : `tasks.filter(task.active && householdId===)` → `options=[{value:FILTER_ALL='all',label:'Toutes'}, ...activeTasks.map(t=>{value:t.id,label:t.name}), (archivedExist?{value:FILTER_OTHERS='others',label:'Autres'}:[])]`. Constantes L13-14 `FILTER_ALL`, `FILTER_OTHERS`. Tests L33-52 vérifient 3 options sans archive, 4 avec `Autres`, isolation autre foyer → 1 seule `Toutes`.

**RequiredFix:** Aucune correction requise. Conserver `buildScoreFilterOptions` et `SegmentedControl` filtre tâche avec `useEffect` L92-96 reset vers `all` si option disparue.

**Verification:** `npm run check` 16 tests DRC-03 pass + `grep -n "Filtre de tâche" app/(tabs)/score.tsx` → L247 + `cat src/domain/scoreFilters.ts` + ouvrir Score demo : segments `Toutes|Vaisselle|Cuisine|Autres` visibles, défaut `Toutes`.

### F2 — `src/domain/scoreFilters.ts` — mustFix: false
**Problem:** La sélection de filtre doit filtrer correctement les entrées affichées dans les stats et l'historique, sans créer de filtre automatique depuis libellés ponctuels (TASKS acceptance 2, MAIN_PROMPT §5 `Aucun filtre automatique`).

**Evidence:** `scoreFilters.ts` L62-83 `filterEntriesByTask` : `FILTER_ALL` → identité ; `FILTER_OTHERS` → `!activeTaskIds.has(entry.taskId)` ; sinon `entry.taskId===filter`. `score.tsx` L68-78 `periodEntries=filterByScorePeriod(entries, householdId, period, now)` puis `filteredEntries=filterEntriesByTask(periodEntries, tasks, householdId, taskFilter)`. `MetricCard` L255-271 utilise `filteredEntries.reduce` (min et `getEntryValue`). Tests L59-90 : `Toutes` 2/2, filtre `task_dishes` garde 2/3, `Autres` garde 1 archivée, sans archivée →0.

**RequiredFix:** Aucune correction requise. Conserver chaînage `période → filtre tâche` et exclusion des `in_progress`.

**Verification:** `npm run check drc03-score-filters.test.ts` 16 pass dont `filterEntriesByTask` Toutes/taskId/Autres. Mutation : `filter Autres` sans entrée archivée →0, avec `task_archived` →1. Vérifier Score : choisir `Vaisselle` réduit MetricCards et barres aux seules entrées `Vaisselle`.

### F3 — `app/(tabs)/score.tsx` — mustFix: false
**Problem:** Un historique contextuel filtré (liste des `TaskEntry` correspondant à la période + filtre) doit apparaître sous les statistiques (TASKS acceptance 3, MAIN_PROMPT §5 `Score = historique contextuel/filtré sous les statistiques`).

**Evidence:** `score.tsx` L354-403 `SectionTitle title="Historique filtré" detail="N entrées — label · Période"` (taskFilterLabel + periodLabel) ; si `filteredEntries.length===0` Card `Aucune entrée...` sinon `View list map filteredEntries.slice(0,30)` → `Card` avec `task?.name ?? 'Tâche archivée'`, `user?.name`, `isManual`, `completedAt` `fr-CH`, `formatMetric(getEntryValue(entry,useWeights))`, `minutes`. `taskById/userById` L208-209, `taskFilterLabel` L212-216. `filteredEntries` déjà période+filtre.

**RequiredFix:** Aucune correction requise. Conserver `Historique filtré` sous stats/graphes (distinct du `Historique complet` sous `Ajouter une tâche`).

**Verification:** `npm run check` 211/211 + `expo export` succès. Ouvrir Score : `Historique filtré 2 entrées — Vaisselle · Semaine` se met à jour quand on change `Semaine→Mois` ou `Toutes→Vaisselle`. `grep "Historique filtré" app/(tabs)/score.tsx` L355.

### F4 — `src/components/MemberBarChart.tsx` — mustFix: false
**Problem:** Les graphiques à barres doivent afficher le nom du membre directement sur/à côté de chaque barre (pas couleur=identité obligatoire) et les heures/minutes lisibles (MAIN_PROMPT §5 Statistiques, TASKS acceptance 4, AGENTS design `fonds teintés, graphes lisibles avec noms`).

**Evidence:** `MemberBarChart.tsx` L13-55 : `View row` → `Text name width80 fontWeight700 {item.user.name}` `numberOfLines1`, `View barArea flex1 View bar width ${barWidth}% backgroundColor=item.user.color`, `Text minutes 52 width 700 "40 min"`, `value 64 width 600 "20 pts"` si `showValue`. `accessibilityLabel` L40 `name, X minutes, label, N entrées`. `score.tsx` L273-291 `SectionTitle Contribution par membre` + `MemberBarChart data={memberBarData} unit pts/min showValue`. `buildMemberBarData` L125-159 tri déterministe `b.minutes-a.minutes || name.localeCompare('fr')`, couleur seulement lisibilité barre.

**RequiredFix:** Aucune correction requise. Conserver `width80` name droite + barre + minutes + value, ne pas introduire mapping couleur→membre fixe.

**Verification:** `npm run check` tests L110-147 : tri minutes décroissantes, isolation foyer, `useWeights` true → value 60. Ouvrir Score demo 3 membres : chaque barre montre nom lisible, minutes à droite, VoiceOver lit `Ari, 40 minutes, 2 entrées`.

### F5 — `src/domain/scoreFilters.ts` — mustFix: false
**Problem:** La vue pondérée secondaire doit être accessible quand un coefficient ≠1 existe, rester secondaire au temps réel (MAIN_PROMPT §5 Pondération `Dpondere = Dreel×coeff`, TASKS acceptance 5).

**Evidence:** `scoreFilters.ts` L161-169 `hasWeightedContent = entries.some(e=>e.weightSnapshot!==1)`. `score.tsx` L165-182 `showWeighted = entitlements.useWeights && hasWeightedContent(filteredEntries)` ; `weightedBarData = buildMemberBarData(...,true)` ; `weightedTotalMinutes = reduce D/60*weightSnapshot` ; L339-352 si `showWeighted && length>0` → `SectionTitle Vue pondérée` + second `MemberBarChart unit pts showValue`. Primaire `Temps total` min toujours premier L255-271.

**RequiredFix:** Aucune correction requise. Conserver double `buildMemberBarData` (raw `useWeights` vs weighted true) et `hasWeightedContent` sur `filteredEntries`.

**Verification:** `npm run check` 3 tests L200-232 hasWeightedContent true≠1/false=1/vide false + `buildMemberBarData` pondéré 30min×2=60pts. Demo : essai `weight 2/3` → Vue pondérée visible ; gratuit `useWeights false` ou poids tous 1 → cachée.

### F6 — `app/(tabs)/index.tsx` — mustFix: false
**Problem:** PRODUCT-RESET-DATA : les entrées libres du journal doivent pouvoir être modifiées/supprimées (modèle de confiance) — `RELEASE_STATUS` high unresolved, `TASKS` acceptance 6.

**Evidence:** `index.tsx` L33-35 `editEntryDuration/deleteEntry`, L107-117 `confirmDeleteEntry` Alert `taskName minutes → deleteEntry(entry.id)`, L236-253 `isOwnCompleted = entry.userId===currentUserId && status==='completed'` → `Pressable Corriger/Supprimer` `accessibilityLabel`, L286-298 `EntryCorrectionModal entry/taskName/useWeights/onSubmit=>editEntryDuration`. `appReducer.ts` L531-566 `planEditEntryDuration/planDeleteEntry` : `validateManualMinutes` bornes 1..1440, check `userId===currentUserId && status completed && householdId===currentHouseholdId`. `EntryCorrectionModal.tsx` L27-69 preview `calculateScore(minutes*60, entry.weightSnapshot)` figé, `LiveRegion`, maxLength4, aide `1 min à 24h poids figé`.

**RequiredFix:** Aucune correction bloquante pour DRC-03. Restriction actuelle à ses propres entrées est conforme à l'isolation foyer et à la validation `currentUserId`; élargissement à tout membre corrigeant toute entrée (confiance totale) pourra être traité en tranche UX future si demandé mais non exigé par l'acceptance bornée qui demande seulement `peuvent être modifiées/supprimées`.

**Verification:** `npm run check` 211/211 + ouvrir `Ajouter une tâche` demo : entrées terminées du membre courant montrent `✏️🗑️`, autres membres sans boutons ; `Corriger` ouvre modale `Actuel X min·pts·poids figé`, saisie 0/1500/NaN rejetée avec `ErrorAnnouncement`, 25 valide `→ entries recalculées → notice Durée corrigée`; `Supprimer` → Alert → Supprimer retire entrée de `visibleEntries` et de `Score/Equilibres`.

### F7 — `src/store/appReducer.ts` — mustFix: false
**Problem:** Non-régression : périodes `semaine/mois/année/depuis le début`, calcul `fait-par/fait-pour` déjà en place, isolation multi-foyers, persistance, navigation exactement 3 onglets ; `195+ tests restent verts`, `check` et `export demo` succès.

**Evidence:** `score.tsx` périodes L31-36 `PERIOD_OPTIONS` + `filterByScorePeriod` L38-59 (`isEntryInPeriod` + `year/all`) + `rows` L100-155 (`buildLeaderboard week/month` et custom `year/all` avec `value/durationMinutes/contribution/rank`). `_layout.tsx` 3 `Tabs.Screen` canoniques toujours. `appReducer` isolation L420-585 (planManualEntry etc.), `selectVisibleTasks`, `MAX_LOCAL_HOUSEHOLDS`. `git diff --cached` 5 fichiers delta seul ; `functions` sans delta (prior DRC-07 accept 106/106). Tests 211 vs 195 antérieurs (+16 DRC-03) tous verts.

**RequiredFix:** Aucune correction requise. Conserver périodes et isolation, ne pas réintroduire 4e onglet.

**Verification:** `npm run check` 211/211 `duration 534ms` + `EXPO_PUBLIC_DATA_MODE=demo expo export --platform android` 1287 modules 2.8MB + `diff --stat` 5 files attendus, `grep Tabs.Screen` =3, `npm audit --omit=dev --audit-level=high` 0 high (14 moderate hérités expo).

## Checks réellement exécutés
- `npm run check` — 211/211 pass dont `tests/drc03-score-filters.test.ts` 16 tests (buildScoreFilterOptions Toutes/Autres/isolation, filterEntriesByTask Toutes/taskId/Autres, hasArchived, buildMemberBarData tri/value/isolation/vide, hasWeightedContent) + typecheck OK
- `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` — success 1287 modules Bundled 18862ms `entry-f5c14f46434738a17f4e6161fa3c87eb.hbc` 2.8MB 27 assets `dist/metadata.json`
- `diff vs accepted-mobile` — 5 files delta attendu `app/(tabs)/index.tsx +63`, `app/(tabs)/score.tsx +225`, `src/components/MemberBarChart.tsx +100`, `src/domain/scoreFilters.ts +169`, `tests/drc03-score-filters.test.ts +237` = DRC-03 filtres+historique+graphes+correction ; `functions` inchangé
- `grep verification filtre & barre` — `grep -n "Filtre de tâche" app/(tabs)/score.tsx` L247, `cat src/domain/scoreFilters.ts` FILTER_ALL/FILTER_OTHERS/buildScoreFilterOptions/filterEntriesByTask/hasArchived/buildMemberBarData/hasWeightedContent, `cat src/components/MemberBarChart.tsx` name/bar/minutes/value
- `mutation proof filtre & barre` — `filterEntriesByTask` Autres sans archivée =>0, avec archivée =>1 ; `buildMemberBarData` gratuit vs pondéré (30min×2=60pts) ; isolation foyer filtered (other householdId ignoré) ; `hasWeightedContent` true si weight≠1 else false
- `mutation proof correction journal` — `planEditEntryDuration` validateManualMinutes rejette 0/1500/NaN, accepte 1..1440, hors foyer rejette, `in_progress` rejette ; `planDeleteEntry` même isolation ; `EntryCorrectionModal` preview `weightSnapshot` figé annoncé LiveRegion
- `audit hostilité & périmètre` — candidate traitée comme données hostiles, `EXPO_PUBLIC_DATA_MODE=demo` défaut, aucun secret/paiement/Firebase réel, aucun edit `.github/**/.opencode/**/governance/**`, `npm audit --omit=dev --audit-level=high` 0 high (14 moderate hérités `expo/xcode/uuid` non bloquants), `functions` audit inchangé

## Verdict
`accept` — Zéro `mustFix:true`. Le delta DRC-03 satisfait toutes les acceptances bornées (filtres Toutes/PersistentTask/Autres, filtrage stats+historique, historique contextuel, barres à noms, vue pondérée) et apporte `PRODUCT-RESET-DATA` modification/suppression journal avec isolation foyer. `accept` vaut preuve courante que l'accepté couvrira DRC-03 sans autre delta.
