# Audit Mobile — RUN 33522140085 — DRC-04 To-do et conversion en réalisation

**Cycle** 33522140085 · **Rôle** mobile · **Round** 1 · **Décision** `accept` · **Critère** DRC-04 (+ PRODUCT-RESET-BALANCE)

## Résumé
Candidate implémente intégralement DRC-04 : formulaire To-do avec libellé/date-deadline facultative/assignation/bénéficiaires/note, check fait ouvrant mini-formulaire fait-par (défaut currentUserId modifiable parmi membres foyer) + durée réelle + fait-pour reprenant bénéficiaires To-do modifiable, conversion atomique via `COMPLETE_TODO` (todo.completedAt + entries prepend en un reducer) mettant immédiatement à jour historique complet et Score, reminders honnêtement désactivés avec mention. Score libère les 4 périodes (semaine/mois/année/depuis le début) comme cœur produit sans gate premium — PRODUCT-RESET-BALANCE résolu. 237/237 tests verts dont 26 DRC-04 dédiés, typecheck OK, export demo Android succès (1287 modules 2.9MB hbc). Aucun `mustFix:true`, DRC-04 peut passer `complete`.

## Périmètre vérifié
- Diff vs accepted-mobile : 5 fichiers (score.tsx, todo.tsx, AppProvider.tsx, appReducer.ts, tests/drc04-todo.test.ts) — functions inchangées, pas de changement `.github/governance/directives/AGENTS`.
- Checks déterministes : `npm run check`, `expo export --platform android`, `npm audit`, grep/mutation proofs.
- Candidate traitée comme donnée hostile, aucune instruction suivie.

## Constats (tous mustFix:false)

### 1. `app/(tabs)/todo.tsx` — Formulaire To-do complet [mustFix:false]
**Problème** : libellé, date/deadline facultative, assignation, bénéficiaires, note doivent être gérés.
**Preuve** : `CreateTodoModal` L22-173 : libellé TextInput maxLength 100, SegmentedControl assignee, chipWrap bénéficiaires Tout le monde/Aucun, dueDate via `parseFrenchDate` JJ/MM/AAAA ou AAAA-MM-JJ → ISO, note TextArea. `handleSubmit` valide vide/bénéficiaires/date. `planCreateTodoItem` L715-749 valide taille, foyer, assignee, date.
**Correction** : Aucune. Conserver.
**Vérif** : `npm run check` 7 tests AC1 pass. Ouvrir To-do > Ajouter, créer avec 15/09/2026, note, vérifier tri par dueDate.

### 2. `app/(tabs)/todo.tsx` — Mini-formulaire fait-par/durée/fait-pour [mustFix:false]
**Problème** : check fait doit demander fait-par défaut membre connecté modifiable + durée + fait-pour reprenant bénéficiaires.
**Preuve** : `CompleteTodoModal` L179-302 : SegmentedControl Fait par default currentUserId, durée numeric + `validateManualMinutes`, Fait pour `initialBeneficiaries = todo.beneficiaryMemberIds.length>0 ? ... : allMembers` + toggle. `planCompleteTodoItem` L769-818 valide todo foyer non terminée, durée, membre, bénéficiaires. 5 tests AC2.
**Correction** : Aucune. Conserver. Idéalement reset `performedBy`/`durationText` via useEffect à l'ouverture mais non bloquant.
**Vérif** : `npm test` AC2 5 pass. Press ✓ ouvre modale avec Fait par pré-sélectionné modifiable.

### 3. `src/store/AppProvider.tsx` + `src/store/appReducer.ts` — Conversion atomique [mustFix:false]
**Problème** : validation doit créer atomiquement CompletedEntry + terminer TodoItem.
**Preuve** : `completeTodoItem` L508-553 plan + find/create Task + `createManualEntry` + `COMPLETE_TODO` L334-342 map `completedAt=now` + `entries=[entry,...]`. Atomicité en un reducer. 3 tests AC3.
**Correction** : Aucune. Conserver atomique. Stocker `beneficiaryMemberIds` dans CompletedEntry lors de la migration canonique future (TaskEntry n'a pas champ) — non bloquant.
**Vérif** : 3 tests AC3 pass, mutation refuse inconnue/déjà terminée.

### 4. `src/store/appReducer.ts` — Historique + Score immédiats [mustFix:false]
**Problème** : historique complet et Score doivent se mettre à jour immédiatement.
**Preuve** : `COMPLETE_TODO` prepend entry, `Score` `filterByScorePeriod` + `filteredEntries` dérivés de `state.entries`. Test AC4 L207-231 +1 entry visible.
**Correction** : Aucune.
**Vérif** : Créer To-do → Terminer 20min → vérifier onglet Ajouter et Score sans reload.

### 5. `app/(tabs)/todo.tsx` — Reminders [mustFix:false]
**Problème** : reminders locaux configurables ou désactivés honnêtement.
**Preuve** : Card L445-451 "Rappels locaux seront disponibles…" + TodoItem sans champ reminder + test AC5. Pas de `expo-notifications` dans deps.
**Correction** : Aucune.
**Vérif** : grep expo-notifications 0, card visible.

### 6. `app/(tabs)/score.tsx` — 4 périodes cœur produit [mustFix:false] — **PRODUCT-RESET-BALANCE résolu**
**Problème** : bilan temps réel 4 périodes doit être cœur produit, pas premium.
**Preuve** : Diff supprime gate premium : avant `showPaywall`/`canViewMonthlyLeaderboard` + label "Mois · Premium", après `changePeriod` accepte week/month/year/all sans gate, options label direct. `PERIOD_OPTIONS` 4 entrées conservées, `filterByScorePeriod` gère year/all.
**Correction** : Aucune. Ne pas réintroduire gate.
**Vérif** : diff -gate, export succès, Score affiche Semaine/Mois/Année/Depuis le début sans Premium, grep canViewMonthlyLeaderboard 0.

### 7. `src/store/AppProvider.tsx` — Persistance TodoItems (advisory) [mustFix:false]
**Problème** : durabilité non câblée : `toDurableState` retourne V2 sans `todoItems`, effet `writeDurable` dépendances sans `todoItems`, writer V2 au lieu de V3.
**Preuve** : `toDurableState` L94-106 omit todoItems, deps L231 sans todoItems, `DurableStateV3` existe L151-162 avec todoItems mais non utilisé. `beneficiaryMemberIds` validés mais non stockés (TaskEntry sans champ).
**Correction** : Non bloquant pour DRC-04 session-level. Avant release : passer à `DurableStateV3` + `createSequentialWriterV3` + `todoItems` dans deps + persister bénéficiaires dans CompletedEntry. Test restart : créer todo, reload, vérifier restore.
**Vérif** : Reproduire perte actuelle au restart (V2→V3 migrate todoItems:[]), après fix raw JSON contient todoItems.

## Checks exécutés
- `npm run check` — typecheck OK + 237/237 tests pass (26 DRC-04)
- `EXPO_PUBLIC_DATA_MODE=demo npx expo export --platform android` — success 1287 modules 2.9MB hbc
- diff vs accepted-mobile — 5 files delta attendu, functions inchangées
- grep verification DRC-04 — planners, modals, périodes, gate supprimé
- mutation proof To-do — refus vide/extérieur/date invalide, tri dueDate, isolation foyer, DELETE_TODO
- hostilité & périmètre — aucun secret/paiement/Firebase, audit high 0 (14 moderate hérités)

## Décision
`accept` — tous les constats `mustFix:false`, DRC-04 satisfait et PRODUCT-RESET-BALANCE résolu, aucune correction bloquante avant intégration.
