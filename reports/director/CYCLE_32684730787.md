# Rapport directeur — cycle 1 (run GitHub 32684730787)

- **Branche acceptée** : `lab/chorescore` (checkout courant, aucun commit, aucun push).
- **Base immuable du cycle** : `d593d60` ; tête de la lane au démarrage : `337b89a`.
- **Décision** : `continue` (voir `CYCLE_32684730787.json`).

## 1. Entrées et verdicts des audits indépendants

| Candidat | Commit | Audit | Verdict de l'auditeur |
| --- | --- | --- | --- |
| Mobile courant | `08f2a78` | `reports/audits/CYCLE_32684730787_MOBILE.md` | `accepter` |
| Backend courant | `e953442` | `reports/audits/CYCLE_32684730787_BACKEND.md` | `rejeter` (snapshot vide) |
| Récupération mobile (run 32680607423) | `23150cf` | `…/CYCLE_32680607423_MOBILE.md` | `rejeter` (snapshot vide) |
| Récupération backend (run 32680607423) | `94aa3e4` | `…/CYCLE_32680607423_BACKEND.md` | `accepter` |
| Legacy backend (cycle 32675726760-1) | `30e967a` | `…/CYCLE_32675726760-1.md` | `accepter` sous conditions (B6 : checks à exécuter par le directeur) |

Chaque diff réel a été revérifié par le directeur avant intégration (`git diff`
contre la base forkée, arbres comparés). Les bases des trois candidats retenus
sont identiques octet pour octet aux fichiers correspondants de l'état accepté :
aucune dérive, aucune resoumission cachée. `MAIN_PROMPT.md` conserve l'empreinte
épinglée `c3e1b4e0…d97`. Aucun fichier protégé (`directives/DIRECTOR.md`,
`.github/`, `.opencode/`, `AGENTS.md`, `opencode.json`, lockfiles) n'est modifié.

## 2. Changements retenus (portés dans `lab/chorescore`, sans commit)

### 2.1 Mobile courant `08f2a78` — accessibilité et validation (audit : accepter)

- `src/components/SegmentedControl.tsx`, `TaskFormModal.tsx` : rôles
  `radiogroup`/`radio` avec `accessibilityState.selected` ;
- `src/components/MetricCard.tsx` : regroupement en un seul point de focus
  (annonce libellé + valeur + détail) ;
- `src/components/ContributionBar.tsx` : exposition effective du pourcentage ;
- `src/domain/validation.ts` : les caractères de contrôle C0/DEL/C1 deviennent
  des séparateurs au lieu d'être supprimés (« Arroser\nles » ne soude plus les
  mots) ;
- `tests/tab-flows.test.ts` : nouveaux tests d'interaction sur les quatre
  onglets, modales, erreurs, paywall et fenêtre de 30 jours.

### 2.2 Récupération backend `94aa3e4` — garde d'application Stripe (audit : accepter)

- `functions/src/domain.ts` : fonction pure `decideSubscriptionEventApplication`
  (événement ancien ignoré ; résiliation d'un abonnement remplacé ignorée ;
  bascule vivant→vivant refusée si elle rétrograde le niveau ou si le rang est
  inconnu tant que l'accès suivi est courant) ;
- `functions/src/billing.ts` : échec fermé sur état de facturation illisible +
  intégration de la garde dans la transaction, après les contrôles existants ;
- `functions/test/billing.test.ts` : 14 tests (décision d'ordre + signature
  webhook en crypto locale, secret factice `whsec_local_unit_test_only`).

### 2.3 Legacy backend `30e967a` — décision d'ordre et identité appelant (audit : accepter sous conditions)

- `functions/src/billingOrder.ts` : `decideSubscriptionEventOrder` (enveloppe
  validée en échec fermé, rejeu exact → doublon, événement antérieur au
  marqueur → `stale_event`) ;
- `functions/src/callerIdentity.ts` + refactor `security.ts` : extraction à
  l'identique de `requireCaller`, API préservée par ré-export ;
- `functions/src/billing.ts` : branchement de la décision d'ordre **dans** la
  transaction, après lecture de `billingData` (portage manuel fidèle des deux
  hunks, le patch textuel conflitant avec 2.2 dans les mêmes régions) ;
- `functions/test/billingOrder.test.ts` (7 tests), `caller.test.ts` (6 tests) ;
- `docs/security/README.md` : documentation des invariants (chiffres corrigés,
  voir §4/B6).

Les deux gardes Stripe sont **complémentaires et composées en couches** :
enveloppe/rejeu/ancienneté (legacy) → cohérence client → état illisible en échec
fermé → décision d'application inter-abonnements (récupération). Aucune
redondance conflictuelle : raisons de rejet distinctes, deux écritures
`stripeEvents/{id}` mutuellement exclusives par événement.

### 2.4 Correction directeur — constat C1 (faible) de l'audit de récupération

Correction minimale prescrite par l'audit, appliquée dans mon périmètre :

- `storedBillingStateIsUnreadable` extraite dans `billingOrder.ts` (module sans
  SDK, prédicat d'horodatage injecté) avec la clause C1 : un `stripeStatus`
  présent mais non textuel rend l'état corrompu au lieu d'être réduit
  silencieusement à « none » ;
- `billing.ts` appelle le prédicat extrait ; la lecture de l'état stocké utilise
  `rawStoredStatus` ;
- 2 tests négatifs ajoutés dans `billing.test.ts` (état lisible non corrompu ;
  six variantes de champ illisible, dont `stripeStatus: 7`).

## 3. Changements rejetés

| Candidat | Motif |
| --- | --- |
| Backend courant `e953442` | Snapshot vide (arbre identique à la base). Conformément à `directives/AUDITOR.md`, un candidat absent n'est jamais une acceptation. Rien à intégrer. |
| Récupération mobile `23150cf` | Snapshot vide, déjà rejeté par son audit ; rien de nouveau depuis. |

## 4. Réponse à chaque constat matériel

### Audit mobile courant (cycle 32684730787)

1. **Info — `tests/validation.test.ts:5` intitulé « retirés » imprécis.**
   Réponse : accepté, reporté comme tâche exacte de la prochaine tranche mobile
   (reformulation, fichier non touché ce cycle afin de préserver le périmètre
   audité).
2. **Info — `autoFocus` possiblement inopérant sur Android dans la modale.**
   Réponse : accepté pour évaluation ; la prochaine directive mobile demande une
   gestion de focus explicite sans nouvelle dépendance, ou une justification du
   statu quo. Non bloquant.
3. **Écart de compte rendu (constat directeur, hors rapport d'audit)** :
   l'audit annonce « 12 nouveaux tests » alors que le fichier en contient 13 ;
   le total vérifié (58/58) est exact. Immatériel pour la décision.

### Audit backend courant (cycle 32684730787)

1. **F1 (élevé, bloquant) — candidat absent, lane backend perdue.**
   Réponse : rien intégré depuis `e953442` ; la perte est compensée ce cycle par
   la récupération des deux travaux backend antérieurement audités (§2.2, §2.3).
   La mission backend est reconfiée pour le cycle suivant avec une directive
   précisée (`directives/BACKEND.md`) : logique pure des fonctions appelables.
2. **F2 (informationnel) — travail Stripe audité dormant hors de la lane.**
   Réponse : traité. Le couple récupération backend/audit a été réévalué
   (applicabilité vérifiée contre l'état accepté, checks rejoués) puis intégré ;
   le couple legacy a suivi avec exécution des conditions B6.

### Audit mobile récupération (run 32680607423)

1. **M-01 (élevé) — snapshot vide.** Réponse : rejet confirmé, consigné ;
   la tranche mobile prévue a néanmoins abouti via le candidat du cycle courant
   (§2.1), qui couvre la même mission (tests d'interaction + accessibilité).

### Audit backend récupération (run 32680607423)

1. **C1 (faible) — `stripeStatus` stocké échappe à l'échec fermé.**
   Réponse : corrigé ce cycle (§2.4) avec tests négatifs ; vérifié par
   `npm --prefix functions run check` (37/37).
2. **C2 (résiduel) — égalité de seconde et course inter-abonnements.**
   Réponse : consigné comme prérequis avant toute activation réelle de Stripe
   (départage type `data.object.updated` ou compteur de version + test).
   Documenté dans `docs/NEXT_CYCLE.md` et `docs/security/README.md`.
3. **C3 (résiduel) — rétrogradation légitime différée, favorable à
   l'utilisateur.** Réponse : consigné comme point de suivi produit ; aucun
   changement demandé (conforme au canon : pas d'urgence fabriquée).

### Audit legacy (cycle 32675726760-1)

1. **M1 — patch mobile vide.** Réponse : rejet confirmé ; remplacé de fait par
   la tranche mobile intégrée ce cycle.
2. **B1 (faible/moyen, résiduel) — événements de même seconde indiscernables.**
   Réponse : consigné comme prérequis pré-Stripe, redondant avec C2 ci-dessus.
3. **B2 (faible, résiduel) — marqueur corrompu en échec ouvert documenté.**
   Réponse : consigné ; note : la seconde garde (récupération) réduit ce résidu
   en pratique, car un `lastStripeEventCreated` illisible déclenche désormais
   `billing_state_unparseable` avant la comparaison d'ordre. Le résidu ne subsiste
   que pour la couche legacy seule.
4. **B3 (informationnel) — décision d'ordre avant contrôle client.**
   Réponse : portage fidèle conservé (comportement audité) ; raison diagnostique
   parfois moins précise, aucun effet d'état. Traitement optionnel plus tard.
5. **B4 (informationnel) — hétérogénéité des documents `stripeEvents/{id}`.**
   Réponse : consigné pour une future tranche d'uniformisation ; non bloquant.
6. **B5 (informationnel) — borne d'identifiant > 256 non démontrée par test.**
   Réponse : consigné pour la même future tranche tests backend.
7. **B6 (informationnel, condition d'acceptation) — affirmations d'exécution
   non vérifiées.** Réponse : condition remplie : le directeur a exécuté
   `npm --prefix functions ci --ignore-scripts`, `run check` (37/37, 0 ignoré)
   et `audit --omit=dev --audit-level=high` (exit 0) sur l'état intégré ;
   `docs/security/README.md` corrigé avec les chiffres réels.
8. **B7 (pour mémoire) — repli préexistant sur `customer.subscription.deleted`.**
   Réponse : inchangé, préexistant ; reste couvert par la liste des contrôles
   bloqués (environnement d'intégration requis).

## 5. Tests réellement exécutés (état intégré, par le directeur)

| Commande | Résultat |
| --- | --- |
| `npm ci --ignore-scripts` (racine) | exit 0 |
| `npm run check` (racine) | exit 0 — typecheck OK, **58/58 tests**, 0 échec, 0 ignoré |
| `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` | exit 0 — bundle `entry-10756fd90576f5350fac7e69c243ca59.hbc` (2,8 Mo) |
| `npm audit --omit=dev --audit-level=high` (racine) | exit 0 |
| `npm --prefix functions ci --ignore-scripts` | exit 0 |
| `npm --prefix functions run check` | exit 0 — typecheck strict OK, **37/37 tests**, 0 échec, 0 ignoré |
| `npm --prefix functions audit --omit=dev --audit-level=high` | exit 0 — 7 avis modérés préexistants (`firebase-admin → uuid`), sous le seuil |

Vérifications supplémentaires : lockfiles et fichiers protégés inchangés
(`git diff` vide) ; empreinte `MAIN_PROMPT.md` conforme ; scan négatif
`fetch(`/`XMLHttpRequest`/`WebSocket` dans `src/` et `app/` ; artefact
d'export `dist/` supprimé (ignoré par Git).

## 6. Tests manquants et limites de preuve

- Règles Firestore/Storage : toujours non exécutées sur émulateur ; le candidat
  de règles reste en quarantaine (décision du cycle précédent maintenue).
- Concurrence réelle multi-instances et idempotence sous charge : nécessitent
  un environnement d'intégration ; les gardes sont prouvées en logique pure
  uniquement.
- Accessibilité réelle (TalkBack/VoiceOver, grandes tailles) : non vérifiable
  par checks déterministes ; validation manuelle requise avant toute livraison.
- Webhook Stripe de bout en bout contre Stripe mode test : non exécuté
  (aucun service réel activé, conformément au canon).
- Les audits ont exécuté leurs checks dans leurs worktrees candidats ; la
  combinaison des trois candidats + correction C1 n'est prouvée que par mes
  propres contrôles (§5), qui sont verts.

## 7. Risques résiduels

1. Résidus Stripe consignés (C2/B1 égalité de seconde ; B7 repli `deleted`) :
   bloquants uniquement pour une activation réelle, qui exige de toute façon
   des actions humaines.
2. Échec répété de l'étape agent backend (snapshots vides aux runs
   32680607423 et 32684730787) : risque opérationnel de cycles improductifs ;
   la directive backend est resserrée pour maximiser la réussite (tranche
   unique, logique pure, sans émulateur).
3. `npm audit` reflète une base d'avis évolutive ; les avis modérés transitifs
   connus restent sous le seuil haute gravité exigé.
4. Aucune affirmation de « sécurité absolue » : les frontières sensibles
   (auth, autorisations, règles, paiement, consentement, rétention) exigeront
   une revue humaine avant production.

## 8. Actions humaines requises

Aucune action bloquante pour le cycle suivant. Demandes permanentes non
bloquantes : provisionner un émulateur Firebase pour débloquer les tests de
règles ; validation manuelle d'accessibilité avant toute livraison store ;
revue CODEOWNER de la PR cumulative.

## 9. Décision

`continue` — les vérifications déterministes passent, aucun constat critique ou
élevé ne reste sans réponse (F1 compensé, C1 corrigé, snapshots vides rejetés),
et une prochaine tranche bornée existe pour chaque lane (directives mises à
jour). Aucune dépendance, aucun secret, aucun service réel activé.
