# Rapport directeur — cycle 32692689814

Run GitHub : 32692689814 · branche acceptée : `lab/chorescore` (base `decc239`,
héritière de l'état accepté `5b3aebc`) · limite de cycles : 0 (illimitée).

## 1. Décision d'intégration par rôle

Aucun repair n'ayant été demandé ce cycle (répertoires
`*_repaired` et `*_audit_*_final` absents/vides), seule la règle 2 s'applique :
candidat original + audit round 1 apparié. Les copies des audits persistées
dans le checkout sont identiques octet pour octet à celles des worktrees
d'audit (`diff` vérifié).

| Rôle | Candidat | Audit apparié | Schéma JSON | cycle/rôle/round | Décision | Intégration |
| --- | --- | --- | --- | --- | --- | --- |
| mobile | `/tmp/chorescore_mobile` = `be11a8a` (parent `5b3aebc`) | `CYCLE_32692689814_MOBILE.json` (worktree `e4d8b00`) | valide (v1) | `32692689814`/`mobile`/`1` | `accept` | **OUI** |
| backend | `/tmp/chorescore_backend` = `7c39e4f` (parent `5b3aebc`) | `CYCLE_32692689814_BACKEND.json` (worktree `ad87b52`) | valide (v1) | `32692689814`/`backend`/`1` | `accept` | **OUI** |

- Aucune version repaired ni final audit n'existe : la règle 1 (supersession)
  est sans objet, aucune double intégration possible.
- Recovery cycle : aucun artefact (`found=false` partout) : rien à traiter.
- Legacy (`/tmp/chorescore_legacy_backend` = `30e967a`, audit prose
  `CYCLE_32675726760-1.md` sans JSON) : non intégrable en l'état (règle 6).
  Réévaluation : `git diff --stat 30e967a..decc239` ne montre que des
  ajouts postérieurs (durcissements `taskCompletion`, `billing`, etc.) — le
  travail legacy reste strictement contenu dans l'état accepté. Close sans
  réintégration, comme au cycle précédent.

## 2. Inspection réelle des diffs (entrées hostiles)

- Mobile `5b3aebc..be11a8a` : 4 fichiers (+230/−28), strictement `src/` et
  `tests/` — `ManualEntryModal.tsx`, `TaskFormModal.tsx`,
  `formFeedback.ts` (nouveau), `form-feedback.test.ts` (nouveau). Aucun
  fichier interdit, aucune dépendance, aucun réseau.
- Backend `5b3aebc..7c39e4f` : 3 fichiers (+1432/−74), strictement
  `functions/src/**` et `functions/test/**`. Aucune dépendance ni lockfile.
- `sha256sum MAIN_PROMPT.md` identique dans les deux candidats et le checkout
  accepté (`c3e1b4e0…d97`).
- Aucun `.skip/.todo/.only/xit/xdescribe` dans les tests des deux candidats.
- `functions/src/invitations.ts` n'importe que `sha256` de `./domain` : module
  pur sans SDK Firestore ni accès réseau ; gardes Stripe intactes ;
  condensat `inviteDigest` = SHA-256 du jeton brut (256 bits, 43 caractères
  base64url), seul le condensat désigne le document stocké.

## 3. Réponse à chaque constat matériel

### Audit mobile (round 1, décision `accept`)

- **MOB-C3-F1 (faible) — précédent fabriqué « motif TaskRow »** :
  `requiredFix` appliqué par le directeur. Les commentaires des deux modales
  décrivent désormais le mécanisme pour lui-même (jeton → key fraîche →
  re-annonce Android) et la différence exacte `assertive`/`polite`, sans plus
  aucune référence à un motif TaskRow inexistant. Vérification de l'audit
  remplie par relecture des deux fichiers corrigés.
- **MOB-C3-F2 (faible) — `onShow` sans garde de visibilité** :
  `requiredFix` appliqué par le directeur. Un ref miroir `isOpenRef`
  (mis à jour à chaque rendu avec `task !== null` / `visible`) conditionne le
  focus dans `onShow` ; plus aucun focus d'un TextInput monté mais invisible,
  quelle que soit la plateforme. Vérification demandée (« test de condition
  de données ou inspection ») : remplie par inspection — la logique de ref
  n'est pas une condition de données extractible sans sur-ingénierie ; le
  risque résiduel UI reste couvert par la dette MOB-C3-F3.
- **MOB-C3-F3 (info) — câblage UI non couvert par test automatisé** :
  constat exact et honnêtement borné par l'auditeur. Reporté : des tests de
  composants exigeraient une dépendance nouvelle (interdite sans
  justification ni revue). La résolution passe par une validation manuelle
  TalkBack/VoiceOver consignée — action humaine listée ci-dessous. Porté dans
  `directives/MOBILE.md` (section dettes) et `docs/NEXT_CYCLE.md`.

### Audit backend (round 1, décision `accept`)

- **F1-identite-decorative-cablage (faible) — portes d'identité décoratives** :
  constat exact. L'audit offre deux résolutions ; je retiens la voie code pour
  le prochain cycle plutôt qu'une simple consignation : la mission prioritaire
  de `directives/BACKEND.md` impose de transmettre aux décisions pures les
  valeurs réellement observées (`request.auth !== undefined`,
  `request.app !== undefined`, `request.auth?.token.email_verified === true`)
  avec tests négatifs prouvant que les portes s'exécutent via le câblage.
  En attendant, la consignation minimale exigée par l'audit est faite ici et
  dans `docs/NEXT_CYCLE.md` : toute modification de `requireCaller` exige une
  revue croisée avec `invitations.ts`. Aucune faille exploitable aujourd'hui
  (`callerIdentity.ts` exige Auth + App Check + email vérifié).
- **F2-concurrence-sans-emulateur (info) — course de double acceptation non
  testée** : constat exact, même classe que le constat F3 backend du cycle
  précédent (`completeTask`). Consigné comme prérequis pré-production dans
  `directives/BACKEND.md` et `docs/NEXT_CYCLE.md` : test d'émulateur de double
  acceptation concurrente (deux utilisateurs, dont un entre deux foyers,
  exactement une incrémentation de `memberCount`). Non traité ce cycle :
  l'émulateur local n'est pas disponible dans cet environnement et son
  installation distante est interdite.

Aucun constat critique ou élevé ce cycle. Aucun constat d'audit final (aucun
round 2) : la règle 5 de report obligatoire est sans objet ; les reports
ci-dessus sont faits par prudence directoriale.

## 4. Corrections directeur portées après intégration

Intégration par `git cherry-pick -n be11a8a` puis `-n 7c39e4f` (parents
identiques à la base forkée, conflit impossible : périmètres disjoints),
suivi des corrections F1/F2 mobile dans le même périmètre produit. Aucun
commit créé par le directeur : l'état accepté reste à persister par le shell
de confiance.

## 5. Tests réellement exécutés (après intégration + corrections)

| Commande | Résultat |
| --- | --- |
| `npm ci --ignore-scripts` (racine) | OK, lockfiles inchangés |
| `npm run check` (racine) | typecheck vert + **71/71 tests** (63 + 8 nouveaux), 0 échec, 0 ignoré |
| `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` | export réussi, bundle ~2,8 Mo, démo hors ligne intacte |
| `npm audit --omit=dev --audit-level=high` (racine) | exit 0, aucune vulnérabilité haute/critique |
| `npm --prefix functions ci --ignore-scripts` | OK, lockfile inchangé |
| `npm --prefix functions run check` | build TS vert + **93/93 tests** (57 + 36 nouveaux), 0 échec, 0 ignoré |
| `npm --prefix functions audit --omit=dev --audit-level=high` | exit 0, aucune vulnérabilité haute/critique |

## 6. Tests manquants / non exécutables ici

- Tout test d'émulateur Firestore (règles, isolation réelle, concurrence
  d'invitations et de `completeTask`) : environnement absent, installation
  distante interdite.
- TalkBack/VoiceOver et contrastes sur appareil : hors portée déterministe.
- Comportement `onShow` réel iOS/Android (y compris fermeture pendant
  l'ouverture) : couvert par inspection et conditions de données, pas par un
  test de composants.

## 7. Risques résiduels

1. Câblage UI des annonces d'erreur non prouvé par test automatisé
   (MOB-C3-F3) — atténué par sélecteurs purs testés et vrais validateurs.
2. Portes d'identité décoratives côté câblage invitations jusqu'à la mission
   backend du prochain cycle (F1) — défense réelle assurée aujourd'hui par
   `requireCaller`/`requireActiveMembershipInTransaction`.
3. Courses transactionnelles non testées sans émulateur (F2 invitations,
   F3 `completeTask` hérité).
4. `npm audit` reflète une base d'avis évolutive : porte sécurité, pas preuve
   d'absence de vulnérabilité.

## 8. Actions humaines requises

- Validation manuelle lecteur d'écran (TalkBack/VoiceOver) avant toute
  livraison store — requis aussi pour clore MOB-C3-F3.
- Revue humaine de la PR cumulative ; fusion interdite aux agents.
- Avant production réelle : émulateurs, règles testées, revue des frontières
  sensibles, puis test d'intrusion indépendant (cf. `SECURITY.md`).

## 9. Décision

**`continue`** : toutes les vérifications déterministes passent, aucun constat
critique/élevé ne reste ouvert, et deux tranches bornées utiles existent
(mobile : synthèses/filtres Historique ; backend : vérité du câblage
d'identité des invitations). Aucune action humaine ne bloque ces tranches.
