# Rapport directeur — cycle 32781937768 (cycle 4)

Run GitHub : `32781937768` · branche acceptée : `lab/chorescore` · base de
départ : `de6470b` · limite de cycles : 0 (illimitée). Décision : **continue**.

## 1. Décision d'intégration par rôle

| Rôle | Candidat | Audit apparié | Round | Décision audit | Intégration |
| --- | --- | --- | --- | --- | --- |
| mobile | `/tmp/chorescore_mobile` → `9d29981` « ChoreScore 32781937768: mobile snapshot » | `reports/audits/CYCLE_32781937768_MOBILE.json` (+ copie identique dans le worktree d'audit) | 1 | `accept` | **Oui** |
| backend | `/tmp/chorescore_backend` → `46fb523` « ChoreScore 32781937768: backend snapshot » | `reports/audits/CYCLE_32781937768_BACKEND.json` (+ copie identique dans le worktree d'audit) | 1 | `accept` | **Oui** |

Règles appliquées :

1. Aucune version repaired ni aucun audit final round 2 n'existaient pour
   quelque rôle que ce soit (`*_repaired` et `*_final` tous vides) : la règle 2
   s'applique — seuls les couples candidat original + audit round 1 ont été
   considérés, jamais deux versions du même travail.
2. Les deux audits sont des JSON présents, de schéma conforme
   (`schemaVersion: 1`, cycle `32781937768`, rôle et round exacts), avec
   `decision` valant exactement `accept`. Aucune prose, aucun statut vert
   amont ni check passant n'a été substitué à cette décision.
3. Aucune divergence cycle/rôle/round, aucun candidat manquant : rien ne
   bloquait l'intégration.
4. Les copies d'audit persistées dans le checkout et celles des worktrees
   d'audit sont identiques octet par octet (`diff` vide).

## 2. Portage réel

- Mobile : `git diff de6470b..9d29981` appliqué proprement —
  `app/(tabs)/history.tsx`, `src/domain/history.ts` (nouveau),
  `tests/history.test.ts` (nouveau).
- Backend : `git diff de6470b..46fb523` appliqué proprement —
  `functions/src/invitations.ts`, `functions/src/invites.ts`,
  `functions/test/invitations.test.ts`.
- Correction directeur ajoutée (voir §4, MOB-C4-F2) :
  `tests/history.test.ts` uniquement.
- Aucun commit, aucun push, aucune fusion, aucun dispatch : l'état accepté
  est laissé au shell de confiance.

## 3. Rejets et récupérations

- **Recovery du cycle échoué 32680607423** : les huit répertoires recovery
  montés étaient vides (aucun candidat, aucun audit, repaired ou final). Rien
  n'était applicable ; rien n'a été intégré de cette source.
- **Preuve legacy** (`/tmp/chorescore_legacy_backend` = `30e967a`, audit prose
  `CYCLE_32675726760-1.md` sans JSON) : non intégrable conformément à la règle
  6 (prose seule insuffisante). Réévaluation faite : `30e967a` est un état
  ancien dont le contenu est strictement contenu dans la branche acceptée
  actuelle (le diff inverse ne montre que le travail déjà porté et audité par
  les cycles intermédiaires 32684730787 → 32692689814). Dossier clos sans
  réintégration, comme au cycle précédent.
- **Aucun rejet de candidat courant** : les deux couples présents étaient
  appariés et explicites.

## 4. Réponse à chaque constat matériel

### Audit mobile (round 1, accept)

- **MOB-C4-F1 (faible) — débordement possible du filtre membre à nombreuses
  options** (`SegmentedControl`, premier usage dynamique jusqu'à 8+ options,
  rangée unique sans wrap ni défilement). **Réponse : reporté en tête de la
  mission mobile du prochain cycle** (`directives/MOBILE.md`). Motif : le
  correctif touche un composant partagé (leaderboard, profile) et la
  vérification exigée est visuelle à grande taille de texte sur petit écran —
  hors portée déterministe du directeur ; une modification non vérifiable
  visuellement aurait déplacé le risque plutôt que le réduire. Le requiredFix
  et la verification de l'audit y sont repris tels quels.
- **MOB-C4-F2 (faible) — frontières année et borne haute non épinglées**.
  **Réponse : corrigé par le directeur ce cycle.** Test nouveau « le changement
  d'année et la borne haute “maintenant” restent exacts » : NOW début janvier
  2027, saisie du 31 décembre 23:59 exclue du mois de janvier, 1er janvier
  00:00 incluse, entrée exactement à `now` comptée une fois, `now+1 s` exclue ;
  assertion semaine cohérente (vendredi 1er janvier 2027, semaine locale
  démarrant le lundi 28 décembre). Vérification d'audit satisfaite : le test
  échoue si `isEntryInPeriod` régresse — démontré par mutation `<=` → `<`
  (7 pass / 1 fail), puis 8/8 après restauration.
- **MOB-C4-F3 (info) — `new Date()` figé dans les mémos** (convention
  préexistante prolongée ; impact limité à la démo). **Réponse : reporté** dans
  la même mission mobile bornée (calculer `now` hors mémo ou jeton temporel
  grossier dans les dépendances), avec documentation de la convention retenue.
  En production, l'heure de référence reste serveur (canon inchangé).

### Audit backend (round 1, accept)

- **F1-cablage-observe-non-epingle (faible) — câblage véridique mais non
  épinglé par test automatisé** (un retour aux constantes passerait les 98
  tests). **Réponse : mission backend prioritaire du prochain cycle**
  (`directives/BACKEND.md`), avec les deux options de requiredFix reprises
  (assertion de source ou extraction d'une composition pure) et la
  verification exigée (démonstration rouge→vert sur revert simulé aux
  constantes). Motif du report : choix de conception appartenant au
  spécialiste, incrément cohérent avec le constat F2 ci-dessous.
- **F2-constantes-identite-tasks (info) — `completeTask` transmet encore des
  booléens constants à `decideTaskCompletion`** (~ligne 300, hors périmètre du
  candidat, branches pures exercées par `taskCompletion.test.ts`). **Réponse :
  fusionnée dans la même mission backend** : généraliser l'extracteur observé
  (par exemple `observedCaller`) à `completeTask` avec tests négatifs de la
  même famille. Aucune faille exploitable aujourd'hui (`requireCaller` amont
  complet) ; défense en profondeur à rendre exécutable partout.

Aucun constat critique ou élevé n'a été émis ce cycle ; aucun constat ne reste
sans réponse.

## 5. Tests réellement exécutés (état intégré, checkout `lab/chorescore`)

| Check | Résultat |
| --- | --- |
| `npm ci --ignore-scripts` (racine) | exit 0, lockfiles intacts (0 diff) |
| `npm run check` (typecheck + tests) | typecheck OK, **79/79 tests** (71 + 7 candidat + 1 correction directeur), 0 échec, 0 ignoré |
| Contrôle de mutation borne haute | mutation `<=`→`<` : 7 pass / **1 fail** (test nouveau) ; restauration : 8/8 |
| `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` | export OK, bundle ~2,8 Mo, exécuté deux fois (après intégration puis après correction test-only) |
| `npm audit --omit=dev --audit-level=high` (racine) | exit 0, 11 modérées préexistantes, 0 haute/critique |
| `npm --prefix functions ci --ignore-scripts` | exit 0, lockfile intact (0 diff) |
| `npm --prefix functions run check` | build OK, **98/98 tests**, 0 échec, 0 ignoré |
| `npm --prefix functions audit --omit=dev --audit-level=high` | exit 0, 7 modérées préexistantes, 0 haute/critique |
| Périmètre | `git status` : exactement les 6 fichiers candidats modifiés/ajoutés + rapports d'audit persistés ; aucun fichier interdit, aucun lockfile, aucune dépendance |
| Démo hors ligne | `grep -rnE "fetch\(|XMLHttpRequest|WebSocket|firebase" src/ app/` : 0 occurrence |

## 6. Tests manquants et limites

- Rendu visuel grande police/petit écran du filtre membre (MOB-C4-F1) : non
  vérifiable déterministe ici — action humaine consignée.
- TalkBack/VoiceOver sur les nouveaux sélecteurs et états : hors portée
  déterministe — validation humaine exigée avant toute livraison (héritée).
- Épinglage automatisé du câblage d'identité backend : absent ce cycle
  (constat F1 backend), programmé comme mission prioritaire suivante.
- Course de double acceptation concurrente et câblage transactionnel de
  `completeTask` : exigent l'émulateur local (résidus consignés).
- Tests de composants UI : toujours interdits sans nouvelle dépendance et
  justification.

## 7. Risques résiduels

- Le filtre membre peut rester difficile à lire pour un foyer nombreux à
  grande taille de texte jusqu'au correctif MOB-C4-F1 (faible, connu, programmé).
- Une session Historique ouverte à travers un changement de semaine/mois peut
  afficher l'ancienne période jusqu'à interaction (info, convention documentée,
  correction programmée).
- Le câblage d'identité des invitations reste véridique mais non épinglé par
  test (faible, programmé) ; `tasks.ts` garde des constantes d'identité côté
  câblage (info, gardes amont complètes, programmé).
- Aucune activation réelle Firebase/Stripe/analytics ; règles Firestore en
  quarantaine ; adaptateur production en échec fermé.

## 8. Actions humaines requises

- Validation manuelle TalkBack/VoiceOver et rendu multi-tailles avant toute
  livraison réelle (aucune urgence pour la démo).
- Revue humaine de la PR cumulative ; fusion interdite aux agents.
- Toute future activation Stripe/Firebase/analytics reste soumise aux portes
  de sécurité et décisions juridiques/produit.

## 9. Décision

**continue.** Justification : toutes les vérifications déterministes passent
sur l'état intégré ; aucun constat critique/élevé ouvert ; deux prochaines
tranches bornées, utiles et non redondantes existent (mobile : robustesse
Historique MOB-C4-F1/F3 ; backend : épinglage du câblage + généralisation à
`completeTask`) ; elles ne requièrent ni secret, ni compte externe, ni
décision humaine, ni nouvelle dépendance. Les directives
`directives/MOBILE.md`, `directives/BACKEND.md`, `directives/AUDITOR.md` et
`docs/NEXT_CYCLE.md` ont été réécrites avec ce travail exact.
