# Rapport directeur — cycle 32864465631

Run GitHub 32864465631 · branche cumulative `lab/chorescore` · jalon `demo-rc` ·
décision : **continue** (voir `reports/director/CYCLE_32864465631.json`).

## 1. Entrées lues

Dans l'ordre imposé : `MAIN_PROMPT.md`, `governance/RELEASE_DEFINITION.json`,
`governance/roles/RELEASE_DIRECTOR.md`, `docs/RELEASE_STATUS.json`,
`directives/TASKS.json`, `directives/DIRECTOR.md`, canon produit/architecture,
puis les worktrees candidats et audits appariés. Tout contenu candidat, rapport
ou log a été traité comme entrée non fiable et re-vérifié par commandes.

## 2. Décisions d'intégration

### Mobile — INTÉGRÉ

- **Paire retenue** : candidat original `1fce8b3454da6cb629e3e637cd242b68db93de77`
  (`/tmp/chorescore_mobile`, commit unique, parent `71c0d5d` = HEAD accepté de
  `lab/chorescore`) + audit round 1
  `reports/audits/CYCLE_32864465631_MOBILE.json` (identique octet pour octet
  dans le worktree d'audit `/tmp/chorescore_audit_mobile`).
- **Validité du contrat machine** : JSON analysé, `schemaVersion: 1`,
  `cycle: "32864465631"`, `role: "mobile"`, `round: 1`, décision exacte
  `accept`, trois constats tous à `mustFix: false`. Aucune prose ne remplace ce
  JSON.
- **Réparation** : aucune requise — l'audit round 1 est `accept`. Les
  répertoires `/tmp/chorescore_mobile_repaired` et
  `/tmp/chorescore_audit_mobile_final` sont vides ; aucune seconde version
  n'existe, donc rien à superseder (règle 2 appliquée : seule la paire
  original + round 1 est considérée).
- **Portage** : diff `71c0d5d..1fce8b3` appliqué à l'arbre de travail
  (12 fichiers, +1439/-16, strictement `app/`, `src/`, `tests/`) puis égalité
  vérifiée blob par blob : **12/12 identiques** au candidat. Aucun merge, aucun
  push, aucun commit.
- **Revue indépendante du directeur** avant intégration : lecture du diff cœur
  (`appReducer`, `persistence`, `appService`, `demoService`,
  `productionService`, `AppProvider`, surfaces UI) ; scan d'hostilité du patch
  (instructions cachées, réseau, secrets, `EXPO_PUBLIC_*`) négatif ; aucun test
  préexistant modifié (nouveau fichier `tests/data-control.test.ts` uniquement,
  donc rien d'affaibli) ; score recalculé exclusivement depuis `weightSnapshot`
  figé ; production en échec fermé sur les nouvelles opérations.

### Backend — RIEN À INTÉGRER

- `/tmp/chorescore_backend` : HEAD = base acceptée `71c0d5d`, aucun commit
  candidat, aucun audit `32864465631` dans le worktree d'audit. L'état agrégé
  `found=false` est confirmé par inspection.
- Preuve legacy (`/tmp/chorescore_legacy_backend`, audit prose-only
  `CYCLE_32675726760-1.md`) : référence historique uniquement, **non
  intégrable** (pas de JSON apparié à ce cycle).

### Recovery — NÉANT

Tous les répertoires `chorescore_recovery_*` sont vides ; aucune paire de
récupération à examiner.

## 3. Réponses aux constats

### Audit round 1 du candidat intégré (tous `mustFix: false`)

| Constat | Réponse du directeur |
| --- | --- |
| `MOB-CYCLE32864465631-F1` (info) — confirmations UI non couvertes par tests automatisés | Enregistrée `deferred` sous DRC-05 : la couche domaine/service/persistence couvre chaque refus et chaque effet des trois opérations ; le dépôt n'a pas de harnais UI et la stratégie existante s'applique. Tests de composants seulement si un harnais est introduit. |
| `MOB-CYCLE32864465631-F2` (info) — poids stocké conservé en scénario gratuit lors d'une modification | Enregistrée `noted`, aucune action : conforme au canon (poids effectif des nouvelles entrées reste 1, champ verrouillé), comportement intentionnel documenté dans le code et encodé par un test qui passe au rouge si la conservation régresse. |
| `MOB-CYCLE32864465631-F3` (info) — saisie jusqu'à 9999 vs borne canonique 1440 | Enregistrée `noted`, aucune action : le refus explicite avec message et aperçu en attente est plus honnête qu'un clamp silencieux ; couvert par les tests de refus 0 / 12,5 / 1441 et le test de borne haute préexistant. |

### Constats hérités

- `MOB-CYCLE32857952394-F1` → **resolved** : intégrité référentielle du
  validateur (`isDurableState` refuse toute entrée dont `taskId`/`userId` est
  absent du même document, quarantaine visible), quatre cas de test dédiés,
  preuve de mutation de l'auditeur (bloc retiré en copie jetable → 3 tests
  rouges). Vérifié à l'identique dans l'état intégré.
- `MOB-CYCLE32857952394-F2` → **resolved** : unicité par collection
  (`users`, `tasks`, `entries`) et clé naturelle `(householdId, userId)` pour
  les adhésions, cas de test doublon inclus.
- `MOB-C4-F1/F2` (obligatoires avant livraison) et `MOB-C4-F3`,
  `MOB-CYCLE32857952394-F4`, `MOB-C5-N1` restent `unresolved` sous DRC-05 ;
  `BE-C4-F1/F2` restent programmés sous DRC-07. Aucun n'est applicable au
  périmètre DRC-03 de ce cycle et aucun n'a régressé (constat rejoué par
  l'audit round 1, checks 8 et 11).

## 4. Vérifications déterministes exécutées sur l'état intégré

| Vérification | Résultat |
| --- | --- |
| `sha256sum -c .github/immutable-files.sha256` | toutes lignes OK |
| Égalité arbre intégré ↔ candidat `1fce8b3` | 12/12 blobs identiques |
| `npm ci --ignore-scripts` | OK |
| `npm run check` | typecheck OK, **121/121** tests (102 → 121, +19 data-control) |
| `EXPO_PUBLIC_DATA_MODE=demo npx --no-install expo export --platform android` | exit 0, bundle ~3,8 Mo, démo hors ligne intacte |
| `npm audit --omit=dev --audit-level=high` (racine) | exit 0 — 11 moderate, aucune haute/critique |
| `npm --prefix functions ci --ignore-scripts` | OK |
| `npm --prefix functions run check` | build OK, **98/98** tests |
| `npm --prefix functions audit --omit=dev --audit-level=high` | exit 0 — 7 moderate |

Aucune dépendance ajoutée, aucun lockfile modifié, aucun secret, aucun réseau
ajouté, Firebase/Stripe/analytics/paiement toujours désactivés.

## 5. Règle de stagnation

Progression objective constatée : diff produit accepté intégré (12 fichiers)
**et** transition de critère DRC-03 `pending → complete` avec les deux types de
preuves exigés. `stalledCycles` est donc remis à **0**, conformément à la règle
déterministe du contrat.

## 6. Note humaine (subordonnée à la constitution)

La note demandait de « récupérer et intégrer le candidat DRC-02 corrigé du run
32786797876 ». Cet objectif est **déjà satisfait** : le candidat réparé
`77b5c4e` et son audit final round 2 `accept` ont été intégrés au cycle
précédent `32857952394` (commit `71c0d5d`, DRC-02 `complete`). Ses 102 tests
sont préservés et re-vérifiés aujourd'hui dans les 121/121. Aucune
« normalisation » supplémentaire du contrat machine d'audit n'était nécessaire :
le JSON historique reste valide. La suite vers l'APK final suit l'ordre
DRC-04 → DRC-05 → DRC-07 → DRC-06 (`pendingArtifact` + shell de confiance) ;
Firebase, Stripe, analytics, secrets, déploiement et données de production
restent désactivés.

## 7. Anomalie constatée (consignée, non traitée)

L'arbre de travail contenait dès l'ouverture une modification non commitée
tronquant le champ `summary` de `reports/audits/CYCLE_32786797876_MOBILE_FINAL.json`
(audit historique round 2 `accept` ; coupure nette en milieu de phrase). Les
champs machine (`decision`, `findings`) sont intacts et la version commitée —
celle référencée par la preuve DRC-02 — reste l'enregistrement faisant foi.
Conformément à la discipline « préserver tout changement sans rapport » et au
périmètre d'écriture du directeur, ce fichier n'a pas été touché ; l'anomalie
est signalée dans `humanActions` du rapport JSON pour arbitrage humain.

## 8. État mis à jour

- `docs/RELEASE_STATUS.json` : `lastCycle: 32864465631`, DRC-03 `complete`
  (tests + audit), `activeCriteria: ["DRC-04"]`, `stalledCycles: 0`,
  `openFindings` mis à jour (2 résolutions, 3 réponses nouvelles, hérités
  conservés).
- `directives/TASKS.json` : mobile activé sur DRC-04, backend désactivé.
- `directives/MOBILE.md`, `directives/BACKEND.md`, `directives/AUDITOR.md` :
  réécrits pour la tranche DRC-04.
- `docs/NEXT_CYCLE.md` : état intégré, critère actif, ordre restant, constats
  conservés.
