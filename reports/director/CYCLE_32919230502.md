# Rapport directeur — cycle 32919230502

Run GitHub 32919230502 · branche cumulative `lab/chorescore` · jalon `demo-rc` · décision : **continue**

## 1. Intégration enregistrée

| Rôle | Candidat | Audit apparié | Décision | Sort |
| --- | --- | --- | --- | --- |
| mobile | `327fba8` (réparé, round 2, parent `e1983a7`, base `0463683`) | `reports/audits/CYCLE_32919230502_MOBILE_FINAL.json` | `accept`, 0 mustFix | **Intégré** par le shell de confiance (manifeste `selections[0]`) |
| mobile (original) | `e1983a7` + audit round 1 (`repair`, F1 mustFix) | `CYCLE_32919230502_MOBILE.json` | `repair` | Non intégré — supersédé par la version réparée (règle 1) |
| backend | absent | absent | — | Non intégré (règle 4) ; preuve legacy prose-only rejetée (règle 6) |
| recovery mobile 32915047376 | `aecbb48` + audit round 1 `accept` | `CYCLE_32915047376_MOBILE.json` | `accept` | **Non intégré** : implémentation concurrente du même critère DRC-04, supersédée par le candidat réparé ; enregistrée, jamais fusionnée avec l'autre version |

Contrôle du manifeste sur le checkout : HEAD = `04636836523ba35d2b15cd9b369a9055a46aaecd`
(= `baseSha`) et surface staged = exactement les 11 fichiers du diff audité
(+1787/-68, strictement `app/`, `src/`, `tests/`). Le JSON final est valide
(schemaVersion 1, cycle/rôle/round appariés, décision exactement `accept`,
six constats tous `mustFix: false`). Aucune double version intégrée.

## 2. Réponse à chaque constat

**Audit round 1 (décision `repair`)**

- **MOB-CYCLE32919230502-F1** (medium, `mustFix: true`) — migration v1→v2 sans
  re-validation de l'état migré, re-persistance d'un document condamné.
  **Résolu** par le candidat réparé : `parseEnvelope` rejoue `isDurableState`
  après `migrateV1ToV2` et refuse en `invalid-shape` (quarantaine visible dès
  la première lecture). Les quatre cas de test exigés + le cas de quarantaine
  première lecture existent ; reproduction indépendante
  (`household_ghost` → `invalid`) et preuve de mutation (retrait de la garde →
  exactement 4 échecs) réalisées par l'auditeur round 2 sur copies jetables.
  Statut `resolved` tracé dans `docs/RELEASE_STATUS.json`.

**Audit final round 2 (décision `accept`, aucun `mustFix`)**

- **F1-R2** : résolution du mustFix confirmée — aucune action.
- **F2-R2** (avertissement trans-foyer nommant le foyer d'origine) : appliqué,
  testé — aucune action.
- **F3-R2** (filtre membre réinitialisé au changement de foyer) : appliqué —
  aucune action.
- **F4-R2** (constante morte `MONTHS_FR` supprimée d'`exportReport.ts`) :
  appliqué — aucune action.
- **F5-R2** (ordinal « 1er » dans `formatDateFr`) : appliqué, assertions mises
  à jour — aucune action.
- **F6-R2** (méta-ligne ReportModal à 4,55:1 mesuré) : appliqué ; la passe
  systématique `textMuted` reste programmée sous DRC-05 — aucune action.

Tous les `requiredFix` de l'audit final valent « Aucun » : **aucune correction
à reporter vers un codeur** au titre de la règle 5. Les constats info du
candidat de récupération non intégré (mémoisation du libellé de période, forme
de `CreateHouseholdResult`) sont tracés dans `docs/NEXT_CYCLE.md` comme pistes
à revérifier sur le code intégré — ils ne concernent pas du code intégré et ne
sont pas élevés en défauts établis.

## 3. État de livraison

- **DRC-04 → complete** avec ses deux types de preuves exigés : tests ciblés
  exécutés par l'auditeur round 2 sur le contenu intégré (tsc OK ;
  76/76 persistence+premium-local+data-control ; 18/18 store-interactions ;
  preuves de mutation) et audit round 2 `accept`. Aucun résultat des checks
  larges n'est revendiqué par le directeur : ils tournent en parallèle sur
  l'arbre audité gelé (règle 7) et échouent fermés si rouges.
- DRC-01/02/03 restent `complete` (aucune preuve retirée, aucun rétrogradage).
- DRC-05 `in_progress`, DRC-06/DRC-07 `pending`.
- `stalledCycles = 0` : diff produit accepté intégré + transition de critère.
- `activeCriteria = ["DRC-05", "DRC-07"]` (deux critères incomplets maximum).

## 4. Prochain travail (missions bornées, périmètres disjoints)

- **Mobile → DRC-05** (~8 fichiers attendus, pas de slices) : MOB-C4-F1
  (filtre membre utilisable petit écran/grandes tailles), MOB-C4-F2 (tests
  frontière année et borne haute `now`), contraste `textMuted` corrigé
  centralement dans le thème (≥ 4,5:1 mesuré par paire + test déterministe).
  Facultatifs bornés : MOB-C5-N1, garde d'hydratation, mémoisation. Pas de
  harnais UI (MOB-CYCLE32864465631-F1 reste reporté).
- **Backend → DRC-07 volet code** (~5 fichiers attendus, pas de slices) :
  BE-C4-F1 (test d'épinglage `observedInviteCaller(request)` avec preuve de
  mutation), BE-C4-F2 (identité observée dans `completeTask`, refus négatifs
  entre deux foyers). Aucun service réel activé.
- Les deux missions tiennent chacune sous ~12 fichiers et n'éditent jamais le
  même fichier : la condition de slices (> ~12 fichiers séparables) n'est pas
  remplie, donc `assignments.*.slices` est volontairement omis.

## 5. Vérifications du directeur

- Manifeste immuable `sha256sum -c .github/immutable-files.sha256` après
  écritures : toutes les lignes OK.
- Aucun fichier produit édité ; seuls `docs/RELEASE_STATUS.json`,
  `directives/TASKS.json`, `directives/MOBILE.md`, `directives/BACKEND.md`,
  `directives/AUDITOR.md`, `docs/NEXT_CYCLE.md` et `reports/director/**` ont
  été modifiés.
- Aucun commit, push, merge, dispatch, déploiement ; aucune dépendance ni
  lockfile touché ; Firebase/Stripe/analytics/secrets restent désactivés.

## 6. Actions humaines

1. Consigne humaine du run concernant le candidat DRC-02 du run 32786797876 :
   déjà satisfaite et tracée (intégration au cycle 32857952394, DRC-02
   complete) — rien à refaire.
2. Toujours ouverte : troncature du champ `summary` de
   `reports/audits/CYCLE_32786797876_MOBILE_FINAL.json` (champs machine
   intacts), à faire statuer par l'humain.
3. La PR brouillon unique et l'attestation finale DRC-06 (APK standalone)
   restent des actes shell/humain, après complétion de DRC-05 et DRC-07.
